# ComfyUI 工作树与这套像素素材方案的 AI 学习解析

日期：2026-04-17

作者说明：这份文档基于 `D:\Astravein_data\creat_art` 中已有的 AGENTS、报告、工作流和 API prompt 做只读分析整理；不改动项目目录，只把学习说明输出到桌面。

---

## 1. 先说结论：这套方案到底在做什么

这不是一套“按一个按钮自动量产最终游戏素材”的方案。

它更准确的定位是：

1. 用 `ComfyUI + 本地 SDXL 体系模型` 先生成可用的视觉方向图。
2. 对场景、地图块、头像这类“结构要求相对宽松”的内容，直接用扩散模型得到高质量参考图。
3. 对角色这类“结构要求非常严格”的内容，不把扩散模型输出直接当最终资产，而是只把它当作方向图或设计参考。
4. 最终真正可进入运行时的角色资产，仍然要回到：
   - `front / side / back`
   - `atlas / slots / skeleton`
   - `Aseprite / Spine` 重建

一句话理解：

这套流程的核心逻辑不是“AI 直接产成品”，而是“AI 负责出方向，人工负责收口成资产”。

---

## 2. 从 AI 学习的角度，先建立一个总脑图

你可以把整个系统看成 4 层。

### 第 1 层：任务层

它要解决的是两类不同问题：

1. 场景类问题
2. 角色类问题

这两类问题虽然都叫“生成图片”，但本质完全不同。

### 第 2 层：模型层

这套方案实际用到的是一个组合模型栈：

1. `Checkpoint`
   - 决定大底子，像“世界知识库 + 画风基础能力”
2. `LoRA`
   - 决定额外风格偏向，像“给模型补课”
3. `IP-Adapter`
   - 决定参考图特征注入，像“看图说话时把图像风格塞进模型”
4. `VAE`
   - 负责图像与潜空间之间的编码和解码
5. `CLIP / CLIP Vision`
   - 负责把文字或图像转成模型能理解的条件信号

### 第 3 层：ComfyUI 工作流层

ComfyUI 不是一个“黑盒式按钮软件”，而是一个**节点图系统**。

它的核心思想是：

1. 每个节点做一件事
2. 节点之间用连线传数据
3. 整张图就是一个有向数据流图

所以你看到的所谓 workflow，本质上就是：

`模型加载 -> 条件准备 -> 采样 -> 解码 -> 保存`

### 第 4 层：资产生产层

模型输出的图片，不一定就是最终资产。

这里有两个层次：

1. 视觉参考图
2. 可运行资产

可运行资产比视觉参考图多了很多约束：

1. 必须统一正侧背
2. 必须能拆件
3. 必须能进入动画系统
4. 必须能维护和批量迭代

这正是为什么角色路线最后会回到 `Aseprite / Spine`。

---

## 3. 为什么场景分支稳定，而角色分支不稳定

这是理解整套方案最重要的一点。

### 3.1 场景分支为什么容易成功

场景、地图块、NPC 头像这类目标有几个特点：

1. 输出是单张图
2. 不要求骨骼结构一致
3. 不要求多视图严格统一
4. 只要构图、色彩、轮廓、材质感对，就已经很有用

所以扩散模型很擅长这类任务。

它擅长“风格合理、视觉好看、整体像那么回事”。

### 3.2 角色分支为什么难

角色不是“画得像”就够了。

你最终要的是：

1. 正面稳定
2. 侧面稳定
3. 背面稳定
4. 头身比稳定
5. 发型、服装、配件位置稳定
6. 未来能拆成 atlas / slots / skeleton

这类问题对模型来说不是单纯的“风格生成”，而是“结构一致性生成”。

扩散模型天然更擅长**视觉统计相似**，不擅长**严谨的资产结构约束**。

所以角色路线里才会出现这样的结论：

1. 多参考板会把拼板布局一起学进去
2. 单角色参考比多参考板好，但仍容易变成设定板
3. `txt2img` 和 `单角色 IP-Adapter` 都没得到可复用优解
4. 因此当前最稳主线是：
   - `方向图 + Aseprite 精修`

这不是退步，而是对模型边界的正确认识。

---

## 4. 你需要先懂的核心概念

这一节按从浅到深来讲。

## 4.1 什么是扩散模型

可以把扩散模型想成一种“学会从噪声里雕刻图像”的模型。

训练时它做两件事：

1. 把干净图片一步步加噪
2. 学会如何反过来一步步去噪

推理时它做的是反方向：

1. 从随机噪声开始
2. 根据提示词和条件一步步去噪
3. 最后得到图像

所以生成图像不是“凭空画出来”，而是“从噪声中逐步收敛出来”。

---

## 4.2 什么是潜空间

Stable Diffusion 并不是直接在原始像素上操作。

它先把图像压缩到一个更小、更抽象的空间，这叫 `latent space`，中文常叫“潜空间”。

你可以把它理解成：

1. 原图是高清完整照片
2. 潜空间像是高度压缩但保留关键结构的表示

模型主要在潜空间里去噪，因为这样速度更快、显存更省。

---

## 4.3 什么是 VAE

`VAE` 在这套系统里主要干两件事：

1. `Encode`
   - 把图片编码成 latent
2. `Decode`
   - 把 latent 解码回图片

所以：

1. `txt2img` 通常是先造一个空 latent，再采样，再解码
2. `img2img` 通常是先把已有图片编码成 latent，再在上面继续采样，再解码

这就是为什么在 `gt_img2img_test_d045.json` 里你会看到：

1. `LoadImage`
2. `VAEEncode`
3. `KSampler`
4. `VAEDecode`

---

## 4.4 什么是 CLIP

`CLIP` 可以理解为“把语言变成语义向量”的编码器。

当你输入提示词：

`fantasy mage portrait, refined pixel art...`

模型不会直接读英文句子，而是先把这段文字编码成一个高维语义表示，再喂给扩散模型。

在 ComfyUI 里这通常对应：

1. `CLIPTextEncode` 正面提示词
2. `CLIPTextEncode` 负面提示词

---

## 4.5 正面提示词与负面提示词

正面提示词告诉模型：

1. 我想要什么
2. 我希望画面朝什么方向收敛

负面提示词告诉模型：

1. 我不要什么
2. 哪些错误模式要尽量压制

例如角色实验里明确把这些放进负面：

1. `character sheet`
2. `multiple views`
3. `reference sheet`
4. `contact sheet`
5. `panel layout`
6. `grid layout`

这说明方案作者已经发现：模型总喜欢往“角色设定板”方向跑，所以需要负面词去压。

这本身就是一次非常典型的“基于误差模式反推提示工程”的 AI 迭代。

---

## 4.6 什么是 Checkpoint

`Checkpoint` 是主模型权重。

它决定：

1. 模型总体画风能力
2. 对人物、场景、材质、构图的默认理解
3. 推理时最核心的知识底座

在这个项目里：

### 场景主线

1. `sd_xl_base_1.0.safetensors`

### 角色分支

1. `animagine-xl-4.0-opt.safetensors`

为什么角色分支要换底模？

因为裸 `sd_xl_base_1.0` 更中性，泛化能力强，但角色气质不够集中。

而 `Animagine XL 4.0 Opt` 是更偏日式角色表现的 SDXL finetune，更接近 Guardian Tales 这种明亮、干净、日式、轮廓稳定的角色风格。

---

## 4.7 什么是 LoRA

`LoRA` 可以理解为一种轻量微调补丁。

它不是完整模型，而是对原模型的“偏向修正”。

你可以把它想成：

1. Checkpoint 是主教材
2. LoRA 是专题补习包

这个项目里的 LoRA 职责很清楚：

1. `pixel-art-xl`
   - 把结果往像素感拉
2. `sxz-duck-game-assets`
   - 把结果往游戏素材感拉
3. `mapchipLora`
   - 专门强化地图块
4. `sdxl_lightning_4step_lora`
   - 提速横评，不是最终定稿

LoRA 可以同时接多个，这本质是在做“风格叠加”和“偏置组合”。

但叠得过多也有风险：

1. 风格互相打架
2. 结构被过度扭曲
3. 模型过拟合到某种表面质感

---

## 4.8 什么是 IP-Adapter

这是角色分支最关键的概念之一。

很多人以为“给模型一张参考图”，模型就会自动只学角色本体。

实际不是。

`IP-Adapter` 的本质是：

1. 把参考图编码成视觉特征
2. 把这些特征作为额外条件注入扩散模型

也就是说，模型学到的不只是“这个角色长什么样”，还可能学到：

1. 画面布局
2. 拼板样式
3. 卡片框
4. 多视图排版

这正是本项目角色路线出现问题的根本原因之一。

多参考板之所以不理想，不是因为 IP-Adapter 没工作，而是因为它工作得太认真了，把你不想学的也学进去了。

---

## 4.9 什么是 CLIP Vision

文本条件用的是 `CLIP`。

图像条件很多时候要用 `CLIP Vision`。

在这个项目里，关键兼容结论是：

1. `ip-adapter-plus_sdxl_vit-h`
2. 必须配 `CLIP-ViT-H-14-laion2B-s32B-b79K.safetensors`

这说明图像适配器和图像编码器之间必须匹配架构。

如果不匹配，就不是“效果差一点”，而可能是：

1. 节点直接异常
2. 特征语义错位
3. 输出质量很差

---

## 4.10 什么是 img2img 和 denoise

`img2img` 不是从纯噪声开始，而是从一张现有图片开始。

流程是：

1. 读取初始图
2. 编码成 latent
3. 对 latent 加入一定强度的噪声
4. 再根据提示词和参考条件重新去噪

其中最关键的控制量就是 `denoise`。

简单理解：

1. `denoise` 小
   - 更像原图
   - 改动更少
2. `denoise` 大
   - 更敢改
   - 更容易偏离原图

你项目里的 `gt_img2img_test_d045.json` 使用了：

1. `denoise = 0.45`

这表示它想做的是：

既保留原参考图的主体结构，又允许模型重新组织细节。

---

## 4.11 什么是 seed、steps、cfg、sampler、scheduler

这些是 ComfyUI 里最常见但也最容易只会调不会懂的参数。

### `seed`

随机种子。

它决定初始噪声长什么样。

同一组模型、提示词、参数下，seed 不同，结果就会不同。

### `steps`

去噪步数。

步数越多，通常收敛更充分，但更慢。

不是越多越好。

太低可能糊，太高可能收益递减，甚至过度修饰。

### `cfg`

Classifier-Free Guidance scale。

可以简单理解为“模型听提示词的力度”。

1. 太低
   - 模型更自由
   - 容易跑偏
2. 太高
   - 过分死听提示词
   - 容易生硬、失真、对比过强

### `sampler`

采样算法。

它决定“每一步怎么走”。

项目里常见的是 `euler`。

它的特点一般是：

1. 速度稳
2. 结果可预期
3. 常用于基础探索

### `scheduler`

噪声调度方式。

它决定每一步的噪声强度如何分配。

项目里常见的是 `normal`。

---

## 5. ComfyUI 到底是什么

ComfyUI 的本质不是“AI 绘图软件”，而是一个**可视化推理编排器**。

它负责把模型推理拆成可以组合的节点。

你可以把它理解成：

1. Photoshop 更像图像编辑器
2. Blender 更像 3D 制作器
3. ComfyUI 更像“推理流程搭建器”

所以它特别适合：

1. 把一套固定工作流沉淀下来
2. 对节点级参数做实验
3. 重用模型栈
4. 通过 API 批量提交任务

---

## 6. 工作流文件和 API prompt 不是一回事

这是你项目里一个很关键的实务点。

AGENTS 已明确写了：

`POST /prompt` 不能直接提交桌面导出的 workflow JSON，必须先改成 API prompt 格式。

这背后的原因是：

### 6.1 桌面导出的 workflow JSON

它是给 ComfyUI 前端编辑器用的。

它里面有：

1. 节点位置
2. 节点大小
3. 连线
4. 分组
5. 画布信息
6. 可视化状态

也就是说，它是“编辑器工程文件”。

### 6.2 API prompt JSON

它是给后端执行器用的。

例如 `gt_img2img_test_d045.json` 的结构就是：

1. 每个节点一个 key
2. `class_type` 指定节点类型
3. `inputs` 指定输入来源和参数

也就是说，它是“执行图描述”。

一句话：

1. workflow JSON 偏前端画布
2. API prompt JSON 偏后端执行

---

## 7. 这套项目里最常见的 ComfyUI 工作树

先看最常见的稳定场景树。

## 7.1 基础 txt2img 工作树

项目中的地图块、NPC 头像、角色头像立绘工作流，本质上都属于这个树型。

### 结构图

```text
CheckpointLoaderSimple
  -> LoraLoader (pixel-art-xl)
    -> LoraLoader (style / mapchip)
      -> CLIPTextEncode (positive)
      -> CLIPTextEncode (negative)
      -> KSampler
EmptyLatentImage -----------^
VAELoader ------------------^
KSampler -> VAEDecode -> SaveImage
```

### 数据流解释

1. `CheckpointLoaderSimple`
   - 载入主模型
2. `LoraLoader`
   - 叠加像素风 LoRA
3. 第二个 `LoraLoader`
   - 叠加场景风格 LoRA 或地图块 LoRA
4. `CLIPTextEncode`
   - 编码正面和负面提示词
5. `EmptyLatentImage`
   - 创建一个空 latent 画布
6. `KSampler`
   - 真正执行扩散采样
7. `VAEDecode`
   - 把 latent 解码成图像
8. `SaveImage`
   - 输出结果

这棵树为什么稳定？

因为它目标单纯：

1. 没有参考图干扰
2. 没有结构迁移难题
3. 只要求生成一张好看的目标图

---

## 7.2 地图块工作树实例

从 `2026-04-11-mapchip地图块工作流.json` 可读出关键配置：

1. Checkpoint
   - `sd_xl_base_1.0.safetensors`
2. LoRA 1
   - `pixel-art-xl.safetensors`
   - 强度 `0.55`
3. LoRA 2
   - `mapchipLora.safetensors`
   - 强度 `0.85`
4. 正面提示词
   - `top down fantasy map tile...`
5. 负面提示词
   - `character, portrait, person...`
6. 画布
   - `1024 x 1024`
7. 采样
   - `steps = 26`
   - `cfg = 6.0`
   - `sampler = euler`
   - `scheduler = normal`

### 从 AI 学习角度怎么理解它

这是一个非常标准的“任务收缩”案例。

它通过三个手段把问题缩小：

1. 提示词只保留 tile 语义
2. 负面词明确排除角色和透视
3. 专用 `mapchipLora` 把模型往地图块分布推

换句话说，它让模型不要想太多，只做“俯视 tileable 地图块”这一件事。

所以稳定。

---

## 7.3 NPC 头像 / 角色立绘工作树实例

这两个工作流结构也几乎一样，只是提示词和尺寸不同。

例如 `2026-04-11-星脉之都区域NPC头像工作流.json`：

1. Checkpoint
   - `sd_xl_base_1.0.safetensors`
2. LoRA
   - `pixel-art-xl` 强度 `0.74`
   - `sxz-duck-game-assets` 强度 `0.68`
3. 正面提示词
   - `fantasy city npc portrait...`
4. 负面提示词
   - `realistic photo, 3d render...`
5. 尺寸
   - `832 x 1216`
6. 采样
   - `steps = 26`
   - `cfg = 6.2`

为什么 `832 x 1216` 这种竖图比例适合头像立绘？

因为它强化了：

1. 单人主体
2. 立式构图
3. 服装轮廓可读性

这说明尺寸本身也是任务设计的一部分，不只是“随手填个数字”。

---

## 7.4 地标场景工作树实例

`landmark_scene` 的固定参数是：

1. 尺寸
   - `1344 x 768`
2. `steps = 28`
3. `cfg = 6.2`
4. `pixel-art-xl = 0.62`
5. `sxz-duck-game-assets = 0.82`

这说明它偏向：

1. 更宽的场景叙事视野
2. 更强一点的风格收束
3. 让游戏素材感比像素颗粒感更强一些

从任务设计上看，这是一条“母图生成”路线：

1. 先把世界观场景氛围定下来
2. 再拆出地图块、区域角色、局部资产

这很像概念设计中的：

1. 先做 world key visual
2. 再做 asset derivation

---

## 8. 角色实验工作树：为什么它更复杂

角色实验的代表样本是：

`D:\Astravein_data\creat_art\outputs\runtime\gt_img2img_test_d045.json`

它不是简单 txt2img，而是：

1. 有角色主 checkpoint
2. 有 LoRA 叠加
3. 有 IP-Adapter 图像参考
4. 有 img2img 初始图
5. 有负面词专门压“设定板布局”

### 结构图

```text
CheckpointLoaderSimple (Animagine XL)
  -> LoraLoader (pixel-art-xl)
    -> LoraLoader (sxz-duck-game-assets)
      -> IPAdapterAdvanced --------------------+
CLIPVisionLoader ------------------------------|
IPAdapterModelLoader --------------------------|
LoadImage (reference image) -------------------+

LoadImage (init image) -> VAEEncode -----------+
CLIPTextEncode (positive) ---------------------+
CLIPTextEncode (negative) ---------------------+
IPAdapterAdvanced output MODEL -> KSampler ----+
KSampler -> VAEDecode -> SaveImage
```

### 这个工作树里每条支路的作用

#### 主模型支路

`Checkpoint -> LoRA -> LoRA`

作用：

1. 先确定“角色底子”
2. 再叠加像素风
3. 再叠加游戏素材风

#### 图像参考支路

`CLIP Vision + IP-Adapter Model + reference image`

作用：

1. 把参考图转成视觉条件
2. 注入到主模型里

#### 初始图支路

`LoadImage -> VAEEncode`

作用：

1. 把已有角色图作为结构起点
2. 不从纯噪声开始

#### 文本条件支路

`positive / negative`

作用：

1. 正面词给出想要的角色描述
2. 负面词阻止模型继续产设定板

#### 采样支路

`KSampler`

作用：

把上述全部条件揉到一起，执行最终生成。

---

## 9. 为什么角色实验容易变成“设定板生成器”

这是整套分析里最值得你真正吃透的地方。

### 9.1 从机器学习角度看，模型并不懂“你真正想保留什么”

模型只会处理统计相关性。

如果你给它的参考图常常长这样：

1. 一个大角色
2. 旁边带头部特写
3. 带部件拆分
4. 带多视图
5. 带卡片框

那么模型学到的就是：

“这种角色风格通常伴随这种布局。”

也就是说，在模型看来：

1. 角色风格
2. 设定板布局

可能不是两回事，而是一个整体分布。

### 9.2 多参考板为什么更严重

报告里已经验证：

1. 多参考板会被模型学成 contact sheet
2. 即使降低 IP-Adapter 权重，也仍强烈倾向拼板

这是因为：

1. 参考中重复出现相似排版模式
2. 模型把它当成“高置信度结构特征”
3. 越多参考，越强化这种布局

### 9.3 单角色参考为什么还是没完全解决

单角色参考虽然减弱了“拼板布局”问题，但仍会出现：

1. 角色设定板
2. 配件展示板
3. 拆件表

说明参考图里还残留了太多“资产说明图”的统计特征。

模型学到的是：

“这种角色通常以说明图的方式出现。”

这不是提示词不够好，而是训练分布和条件注入的结果。

---

## 10. 为什么当前项目的角色策略是合理的

AGENTS 里当前口径是：

1. 先收素材
2. 先做单张正面成品
3. 暂不盲目扩展角色批量生成
4. 主线是 `方向图 + Aseprite 精修`

这套口径从 AI 学习角度是完全合理的。

原因是它承认了扩散模型的边界。

### 10.1 扩散模型擅长的

1. 风格拟合
2. 氛围生成
3. 视觉探索
4. 单张图设计方向

### 10.2 扩散模型不天然擅长的

1. 多视图严格一致
2. 角色部件拓扑稳定
3. 动画可拆件结构
4. 运行时资产约束

所以最佳策略不是硬逼模型一步到位，而是分层：

1. 模型负责“看起来像”
2. 人工负责“结构上能用”

这就是一个成熟工作流和纯试玩模型的区别。

---

## 11. 为什么要把素材分成风格素材、结构素材、动画素材

这也是很典型的机器学习思维。

不同目标对应不同监督信息。

### 风格素材

作用：

1. 学气质
2. 学头身比
3. 学配色
4. 学成品感

### 结构素材

作用：

1. 学 front / side / back
2. 学遮挡关系
3. 学可拆件逻辑

### 动画素材

作用：

1. 学运动方式
2. 学骨骼层次
3. 学逐帧规律

如果你把这三类混着喂，模型就很容易把目标混淆。

这正是为什么当前阶段会强调：

先把“成品感单体 NPC 风格素材”收够，再推进单张正面成品。

---

## 12. 这套方案里的 API 与本地部署逻辑

项目里已确认 ComfyUI 本地 API 入口是：

`http://127.0.0.1:8000`

已验证接口包括：

1. `GET /system_stats`
2. `GET /queue`
3. `GET /history`
4. `POST /prompt`

### 为什么优先走 API

AGENTS 明确写了：

1. 优先走本地 API
2. 不抢前台桌面焦点
3. 不优先做 GUI 自动化

这背后的工程逻辑是：

1. API 更稳定
2. 更适合批量实验
3. 更可记录和复现
4. 不依赖窗口位置和鼠标操作

这说明整个方案并不是“手点 UI”，而是在逐步走向**可复现的推理流水线**。

---

## 13. 为什么外部模型目录设计很重要

项目使用了外部模型根目录：

`D:\Code_box\art`

并通过：

`extra_models_config.yaml`

把 `checkpoints / vae / loras / ipadapter / clip_vision` 等路径映射给 ComfyUI。

这其实是很典型的工程化设计。

好处是：

1. 模型与程序安装目录解耦
2. ComfyUI 重装时不必重搬全部模型
3. 可以按职责组织模型
4. 可以让多个 workflow 共用统一模型库

从长期看，这比把模型全堆进 `C:\apps\ComfyUI\models` 更可维护。

---

## 14. 如何理解“固定参数”

项目里已经沉淀了三组固定参数：

### `landmark_scene`

1. `1344x768`
2. `steps=28`
3. `cfg=6.2`
4. `pixel-art-xl=0.62`
5. `sxz-duck-game-assets=0.82`

### `tile`

1. `1024x1024`
2. `steps=26`
3. `cfg=6.0`
4. `pixel-art-xl=0.58`
5. `mapchipLora=0.9`

### `npc`

1. `832x1216`
2. `steps=26`
3. `cfg=6.2`
4. `pixel-art-xl=0.74`
5. `sxz-duck-game-assets=0.68`

从学习角度看，这不是“拍脑袋经验值”，而是已经经过若干轮实验后形成的**局部稳定区间**。

所谓稳定区间的意思是：

1. 再低，质量明显掉
2. 再高，收益不明显或副作用变大
3. 当前区间在质量、速度、风格约束之间比较平衡

所以“固定参数”其实是在帮你减少实验维度。

---

## 15. 这套方案的真正工作树，不只是 ComfyUI 节点树

如果从更高一层看，这套生产流程还有一个“项目级工作树”。

### 项目级工作树

```text
目标定义
-> 选择分支（场景 / 角色）
-> 选择主模型栈
-> 选择 workflow
-> 组织提示词与参考素材
-> ComfyUI 生成测试图
-> 平铺输出对比
-> 人工横评
-> 确认是否继续扩散
-> 若角色仍不稳，则转 Aseprite / Spine 结构重建
```

这棵树比单纯节点图更重要。

因为很多结果不是由单个节点决定的，而是由上层决策决定的：

1. 你选了什么素材
2. 你把参考定义成风格素材还是结构素材
3. 你是否把模型输出当最终资产
4. 你在哪一步止损

这些才是真正决定项目成败的地方。

---

## 16. 从入门到进阶，你应该怎么理解这套流程

我建议你按 4 个阶段来学。

## 阶段 1：先把 ComfyUI 当成“函数拼装器”

你只需要理解：

1. 节点有输入输出
2. 数据沿连线流动
3. 一条工作流就是一条执行图

这个阶段先看懂：

1. `CheckpointLoaderSimple`
2. `LoraLoader`
3. `CLIPTextEncode`
4. `EmptyLatentImage`
5. `KSampler`
6. `VAEDecode`
7. `SaveImage`

如果你把这 7 个节点看懂了，场景线就已经能读懂大半。

## 阶段 2：理解扩散模型的条件系统

你要能回答：

1. 提示词为什么能控制图像
2. 负面提示词为什么能压错误模式
3. LoRA 为什么会改变风格分布
4. 为什么不同 checkpoint 差别那么大

这个阶段的核心不是调参，而是理解“模型在听谁的话”。

## 阶段 3：理解参考图注入

你要能回答：

1. IP-Adapter 为什么能让结果更像参考
2. 为什么它也会把布局学进去
3. 为什么 CLIP Vision 必须匹配
4. 为什么单角色参考比多参考板更可控

这个阶段会让你真正明白：

参考图不是“给模型看一眼”，而是“把图像分布条件注入模型”。

## 阶段 4：理解资产生产与生成图的差异

你要能回答：

1. 为什么单张好看图不等于游戏资产
2. 为什么角色最终必须回到结构化重建
3. 为什么 front / side / back 比单张美图更重要
4. 为什么当前方案要止损，不继续盲目扩散角色批量生成

这个阶段理解后，你就不会再把扩散图当作唯一答案。

---

## 17. 用最直白的话解释这套角色路线为什么“看起来绕，但其实对”

很多人看到这里会疑惑：

“既然已经有 AI 了，为什么还要 Aseprite 精修、拆件、Spine？”

答案很简单。

AI 解决的是“像不像”的问题。

游戏资产解决的是“能不能用”的问题。

这两个问题不是一回事。

例如一张 AI 角色图可以：

1. 很像 Guardian Tales
2. 很好看
3. 很有气质

但它仍然可能完全不能用于：

1. 侧面补图
2. 背面补图
3. 部件拆分
4. 骨骼动画
5. 批量换装

所以当前项目选择：

1. 先把 AI 用在它擅长的方向图
2. 再把人工用在它必须负责的结构资产

这实际上是非常成熟的生产逻辑。

---

## 18. 你现在应该把几个关键词怎么记住

### `Checkpoint`

主模型，决定底层世界观和画风基础。

### `LoRA`

轻量风格补丁，负责把模型往特定风格拉。

### `VAE`

图像和潜空间之间的编码器/解码器。

### `CLIPTextEncode`

把提示词转成语义条件。

### `CLIP Vision`

把图像转成视觉条件。

### `IP-Adapter`

把参考图特征注入扩散模型。

### `EmptyLatentImage`

创建一张空的 latent 画布，常用于 txt2img。

### `VAEEncode`

把现有图转成 latent，常用于 img2img。

### `KSampler`

核心采样器，负责真正的去噪生成。

### `denoise`

img2img 时控制“保留原图多少、改动多少”的参数。

---

## 19. 如果你以后自己看 workflow，应该按什么顺序读

推荐永远按这 6 步读：

1. 先看 `Checkpoint`
2. 再看叠了哪些 `LoRA`
3. 再看有没有 `IP-Adapter`、`ControlNet`、`img2img`
4. 再看正负提示词
5. 再看尺寸、steps、cfg、sampler、denoise
6. 最后看输出节点和文件名前缀

这样你基本 30 秒内就能判断：

1. 这是场景还是角色
2. 是 txt2img 还是 img2img
3. 是风格探索还是结构控制
4. 这个 workflow 的目标是什么

---

## 20. 这套项目最值得你学会的不是某个节点，而是这种思维

这套方案最值得学的，不是“某个神奇参数”，而是下面这套思维方式：

1. 先把任务分型
2. 再为不同任务选择不同模型栈
3. 用 workflow 固化实验
4. 用 API 提交保证可复现
5. 用横评而不是单张图判断效果
6. 在模型边界处及时止损
7. 把最终资产交给真正适合它的工具链

这就是为什么这份 AGENTS 文档虽然看起来像操作说明，但本质上其实是一份**生成式资产工程方法论**。

---

## 21. 最后的总总结

如果把这整套方案压缩成最重要的 7 句话，可以这样记：

1. ComfyUI 工作流本质是节点组成的数据流图。
2. 场景类任务适合扩散模型直接产出方向图。
3. 角色类任务难点不在风格，而在结构一致性。
4. Checkpoint 决定底子，LoRA 决定偏向，IP-Adapter 决定参考注入。
5. 多参考板之所以失败，不是模型没学会，而是把不该学的布局也学进去了。
6. 当前最稳角色路线是 `单张正面方向图 + Aseprite 精修`。
7. 真正的运行时角色资产，最后仍然要回到 `front / side / back + atlas / slots / skeleton`。

如果你已经看懂这 7 句话，你就已经不只是会“跑 ComfyUI”，而是开始理解这套方案为什么这样设计了。

---

## 22. 附：本次分析主要依据

1. `D:\Astravein_data\creat_art\AGENTS.md`
2. `D:\Astravein_data\creat_art\docs\reports\2026-04-14-guardian-tales-character-pack-runs-report.md`
3. `D:\Astravein_data\creat_art\docs\reports\2026-04-14-guardian-tales-single-character-runs-report.md`
4. `D:\Astravein_data\creat_art\docs\reports\2026-04-13-guardian-tales-model-stack-report.md`
5. `D:\Astravein_data\creat_art\docs\reports\2026-04-13-guardian-tales-model-path-mapping-report.md`
6. `D:\Astravein_data\creat_art\outputs\runtime\gt_img2img_test_d045.json`
7. `D:\Astravein_data\creat_art\outputs\runtime\vibe-sessions\20260414-guardian-tales-structure-r1\phase-structure-extraction.json`
8. `D:\Code_box\art\05_workflows\2026-04-11-星脉之都地标场景母图工作流.json`
9. `D:\Code_box\art\05_workflows\2026-04-11-mapchip地图块工作流.json`
10. `D:\Code_box\art\05_workflows\2026-04-11-星脉之都区域NPC头像工作流.json`
11. `D:\Code_box\art\05_workflows\2026-04-11-角色头像立绘工作流.json`

