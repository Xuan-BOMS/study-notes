# 给只有 C 基础的人看的代码术语入门手册

这份手册的目标不是让你立刻会写所有语言，而是让你在看 Java、Go、Rust、JSON、前端代码时，先知道常见格式到底在干什么。

你可以把它当成一份“读代码词典”。

---

## 1. 先建立总图

无论什么语言，代码大体都在处理这几类东西：

1. 数据放哪里
2. 逻辑怎么写
3. 多个功能怎么组织
4. 不同代码之间怎么配合

对应到常见术语，大致就是：

- 变量：存数据
- 类型：说明数据是什么
- 函数：做事情
- 条件和循环：控制流程
- 结构体/类：组织数据和功能
- 模块/包：组织文件
- 接口：规定能力
- JSON：交换数据的文本格式
- 前端组件：组织页面的方式

如果你只有 C 基础，其实前半部分你已经会了。最需要补的是：

- `class`
- `object`
- `method`
- `constructor`
- `interface`
- `trait`
- `module/package`
- `JSON`
- 前端里的 `component / props / state`

---

## 2. 从 C 视角看常见代码结构

你熟悉的 C：

```c
int add(int a, int b) {
    return a + b;
}
```

别的语言本质一样，只是写法变化了。

Python：

```python
def add(a, b):
    return a + b
```

Go：

```go
func add(a int, b int) int {
    return a + b
}
```

Rust：

```rust
fn add(a: i32, b: i32) -> i32 {
    a + b
}
```

所以你先不要把新语言想得太神秘。大多数时候只是：

- 同样的逻辑
- 不同的语法
- 多了一些组织代码的方法

---

## 3. 基础术语

### 3.1 变量 `variable`

变量就是存数据的名字。

C：

```c
int age = 18;
```

Go：

```go
age := 18
```

Rust：

```rust
let age = 18;
```

JavaScript：

```js
let age = 18;
```

你可以把变量理解成：

“给一块数据起名字，方便后面使用。”

---

### 3.2 类型 `type`

类型说明这个数据是什么。

常见类型：

- `int`：整数
- `float` / `double`：浮点数
- `char`：字符
- `string`：字符串
- `bool`：真假

从 C 的角度看，类型就是“这块内存该怎么解释”。

---

### 3.3 函数 `function`

函数就是把一段逻辑起个名字，以后重复调用。

```c
int max(int a, int b) {
    if (a > b) return a;
    return b;
}
```

你可以把函数理解成：

“输入一些东西，做处理，输出结果。”

---

### 3.4 参数 `parameter`

函数输入的数据。

```c
int add(int a, int b)
```

这里 `a` 和 `b` 就是参数。

---

### 3.5 返回值 `return value`

函数执行后给出的结果。

```c
return a + b;
```

---

### 3.6 作用域 `scope`

变量在哪个范围里有效。

```c
{
    int x = 10;
}
```

出了大括号，`x` 就不能用了。

这点在几乎所有语言里都存在。

---

## 4. `struct`、`class`、`object`

这组概念是从 C 转向别的语言时最重要的一组。

### 4.1 `struct`

你在 C 里最熟悉：

```c
struct Student {
    char name[20];
    int age;
};
```

作用是把多个相关数据打包。

Go 和 Rust 也有 `struct`：

Go：

```go
type Student struct {
    Name string
    Age  int
}
```

Rust：

```rust
struct Student {
    name: String,
    age: i32,
}
```

所以你可以先把 `struct` 记成：

“装数据的一种结构。”

---

### 4.2 `class`

`class` 可以先理解成：

“`struct` 加上和这些数据相关的函数，再加上一些访问控制。”

Java：

```java
class Student {
    String name;
    int age;

    void introduce() {
        System.out.println("My name is " + name);
    }
}
```

这里：

- `name`、`age` 是数据
- `introduce()` 是这个数据相关的行为

所以：

- `struct` 更偏向“打包数据”
- `class` 更偏向“数据 + 功能一起组织”

---

### 4.3 对象 `object`

类是图纸，对象是按图纸造出来的具体东西。

```java
Student s = new Student();
```

这里：

- `Student` 是类
- `s` 是对象

你可以把对象理解成：

“某个类的一个实际实例。”

---

### 4.4 方法 `method`

写在类里面的函数，通常叫方法。

```java
class Dog {
    void bark() {
        System.out.println("wang");
    }
}
```

`bark()` 本质上还是函数，只不过它属于 `Dog`。

所以：

- 普通函数：独立存在
- 方法：属于某个类或类型

---

### 4.5 构造函数 `constructor`

构造函数是在创建对象时自动调用的初始化函数。

```java
class Student {
    String name;
    int age;

    Student(String n, int a) {
        name = n;
        age = a;
    }
}
```

使用时：

```java
Student s = new Student("Tom", 18);
```

你可以把构造函数理解成：

“创建对象时顺便给初值。”

---

### 4.6 `this`

`this` 表示“当前这个对象自己”。

```java
class Student {
    String name;

    Student(String name) {
        this.name = name;
    }
}
```

这里：

- 参数也叫 `name`
- 成员变量也叫 `name`
- `this.name` 表示对象自己的成员变量

这是很多初学者第一次看类时最容易混的地方。

---

## 5. 权限和封装

### 5.1 `public`

表示外部可以访问。

### 5.2 `private`

表示外部不能直接访问，只能类内部使用。

Java：

```java
class Student {
    public String name;
    private int age;
}
```

你可以先简单理解成：

- `public`：公开接口
- `private`：内部细节

---

### 5.3 封装 `encapsulation`

封装就是：

“把数据和操作数据的逻辑包起来，同时限制外部乱改。”

目的不是让代码更复杂，而是防止状态被随便改坏。

---

## 6. 继承、多态、抽象

### 6.1 继承 `inheritance`

一个类在另一个类基础上扩展。

```java
class Animal {
    void eat() {
        System.out.println("eat");
    }
}

class Dog extends Animal {
    void bark() {
        System.out.println("wang");
    }
}
```

这里 `Dog` 自动有 `eat()`，同时再增加自己的功能。

---

### 6.2 多态 `polymorphism`

同一个接口，具体行为可以不同。

比如都叫 `speak()`：

- 狗输出 `wang`
- 猫输出 `miao`

调用的人只管调用 `speak()`，不一定关心具体是哪种动物。

---

### 6.3 抽象 `abstract`

抽象就是先定义规则、能力或者框架，不马上给完整实现。

你可以先把它理解成：

“先说应该有什么，再说具体怎么做。”

---

## 7. `interface` 是什么

这是你刚才重点问到的。

### 7.1 最短定义

`interface` 就是：

“规定某个东西必须提供哪些功能，但不写具体实现。”

可以把它想成“能力清单”或者“协议”。

---

### 7.2 用 C 的视角理解

在 C 里你可能见过函数声明：

```c
int add(int a, int b);
```

它先告诉你“有这个函数”，但不一定在这里实现。

`interface` 像是更大一点的版本：

- 不只声明一个函数
- 而是声明一组必须提供的函数

---

### 7.3 Java 里的 `interface`

```java
interface Animal {
    void speak();
}
```

某个类实现它：

```java
class Dog implements Animal {
    public void speak() {
        System.out.println("wang");
    }
}
```

意思是：

- `Animal` 规定了 `speak()`
- `Dog` 说自己符合这个规则
- 那 `Dog` 就必须把 `speak()` 写出来

---

### 7.4 Go 里的 `interface`

Go 也有 `interface`，但更灵活。

```go
type Speaker interface {
    Speak()
}

type Dog struct {}

func (d Dog) Speak() {
    fmt.Println("wang")
}
```

Go 不需要显式写 `implements`。

只要 `Dog` 有 `Speak()`，它就自动满足 `Speaker`。

所以 Go 的接口可以理解成：

“只看你会不会这个行为，不看你有没有正式声明。”

---

### 7.5 TypeScript 里的 `interface`

前端里经常拿它描述对象结构。

```ts
interface User {
  name: string;
  age: number;
}
```

这里它的意思是：

“User 这种数据必须有 `name` 和 `age`。”

所以在前端里，`interface` 很多时候是“数据形状说明书”。

---

## 8. Rust 里的 `trait`

Rust 没有 `interface` 这个关键字，但有一个很像的概念：`trait`。

### 8.1 基本形式

```rust
trait Speak {
    fn speak(&self);
}
```

实现：

```rust
struct Dog;

impl Speak for Dog {
    fn speak(&self) {
        println!("wang");
    }
}
```

你可以先直接记：

- Java/Go/TypeScript 常见 `interface`
- Rust 对应的相近概念是 `trait`

---

### 8.2 为什么 Rust 不直接叫 `interface`

因为 Rust 的 `trait` 不只是简单接口。

它还能参与：

- 泛型约束
- 默认实现
- 更复杂的类型系统能力

但对于入门阅读来说，你先把它看成“行为接口”就够了。

---

## 9. Rust 常见基础词

Rust 对有 C 基础的人很值得学，因为它也偏底层，但规则更严格。

### 9.1 `let`

定义变量：

```rust
let x = 10;
```

---

### 9.2 `mut`

Rust 默认变量不可改，想改要加 `mut`：

```rust
let mut x = 10;
x = 20;
```

这点和 C 很不一样。

---

### 9.3 所有权 `ownership`

Rust 最核心的概念。

可以先粗略理解成：

“每份数据在某一时刻都有一个主要拥有者。”

Rust 靠这个避免很多内存错误。

---

### 9.4 借用 `borrow`

不拿走数据所有权，只是临时使用。

这像“借来看看，不归你”。

---

### 9.5 引用 `reference`

Rust 里的引用长这样：

```rust
&x
```

你可以把它先类比成“更安全、规则更严格的指针”。

---

### 9.6 生命周期 `lifetime`

引用能活多久。

Rust 要保证引用不会指向已经失效的值。

---

### 9.7 `impl`

给某个类型实现方法，或者给某个类型实现某个 trait。

```rust
impl Dog {
    fn bark(&self) {
        println!("wang");
    }
}
```

---

### 9.8 `enum`

Rust 的 `enum` 比 C 强很多。

C 里枚举通常更像一组整数常量。

Rust 里可以带数据：

```rust
enum Message {
    Quit,
    Move(i32, i32),
}
```

---

### 9.9 `match`

模式匹配，类似更强的 `switch`。

```rust
match x {
    1 => println!("one"),
    _ => println!("other"),
}
```

---

### 9.10 `Option`

表示“可能有值，也可能没有值”。

```rust
Option<i32>
```

常见取值：

- `Some(5)`
- `None`

可以理解成显式处理“空值”。

---

### 9.11 `Result`

表示“成功”或者“失败”。

```rust
Result<T, E>
```

和 C 用错误码有点像，但表达更规范。

---

## 10. Go 常见基础词

Go 的语法相对直接，比较适合从 C 过来的人读。

### 10.1 `package`

Go 每个文件开头通常会写：

```go
package main
```

表示这个文件属于哪个包。

你可以先理解成：

“这个文件在代码组织中的归属。”

---

### 10.2 `import`

导入别的包：

```go
import "fmt"
```

功能上有点像更规范的 `#include`。

---

### 10.3 `struct`

Go 的结构体和 C 很像：

```go
type User struct {
    Name string
    Age  int
}
```

---

### 10.4 方法接收者 `receiver`

Go 没有传统意义上的 class，但可以给类型绑定方法：

```go
func (u User) SayHello() {
    fmt.Println(u.Name)
}
```

这里 `(u User)` 就叫接收者。

你可以理解成：

“这个函数是给 `User` 用的。”

---

### 10.5 `interface`

Go 的接口只关心方法集合，只要方法对得上就行。

这让 Go 很适合写解耦的代码。

---

### 10.6 `error`

Go 常把错误作为返回值：

```go
result, err := doSomething()
if err != nil {
    // 处理错误
}
```

这和 C 的“返回状态码”思路比较接近。

---

### 10.7 `defer`

延迟执行，通常用于收尾：

```go
defer file.Close()
```

你可以把它理解成：

“先登记一下，这个函数结束前记得执行这句。”

---

### 10.8 `goroutine`

Go 的轻量级并发任务。

```go
go doWork()
```

可以先理解成“很轻的并发执行单元”。

---

### 10.9 `channel`

goroutine 之间通信的管道。

```go
ch := make(chan int)
```

这在 Go 里很常见，是并发编程的重要基础。

---

## 11. JSON 是什么

JSON 不是编程语言，而是一种数据格式。

全名是 `JavaScript Object Notation`。

常见用途：

- 接口返回数据
- 配置文件
- 前后端通信
- 保存结构化文本信息

例如：

```json
{
  "name": "Tom",
  "age": 18,
  "isStudent": true
}
```

---

### 11.1 JSON 对象 `object`

大括号包起来的一组键值对：

```json
{
  "name": "Tom"
}
```

---

### 11.2 JSON 数组 `array`

方括号包起来的一组值：

```json
[1, 2, 3]
```

---

### 11.3 键值对 `key-value`

```json
"name": "Tom"
```

左边是键，右边是值。

---

### 11.4 JSON 常见值类型

JSON 支持这些值：

- 字符串：`"hello"`
- 数字：`123`
- 布尔：`true` / `false`
- 空值：`null`
- 对象
- 数组

---

### 11.5 JSON 和 C 的结构体怎么类比

C：

```c
struct User {
    char name[20];
    int age;
};
```

JSON：

```json
{
  "name": "Tom",
  "age": 18
}
```

你可以把 JSON 理解成：

“用文本写出来的数据结构。”

---

## 12. 文件组织相关术语

### 12.1 模块 `module`

模块一般表示一个功能单元。

不同语言里范围略有不同，但你可以先理解成：

“一块被组织起来、可以被别人使用的代码。”

---

### 12.2 包 `package`

包通常是多个相关模块或文件的组织单位。

Go 里很常见，Java 也很多见。

---

### 12.3 导入 `import`

把别处代码拿进来使用。

Python：

```python
import math
```

Go：

```go
import "fmt"
```

TypeScript：

```ts
import { useState } from "react";
```

你可以把它理解成升级版的 `#include`。

---

## 13. 前端基础词

你说也想要前端部分，下面放最常见的。

### 13.1 HTML

网页结构。

```html
<h1>Hello</h1>
<button>Click</button>
```

可以理解成页面骨架。

---

### 13.2 CSS

网页样式。

```css
button {
  color: red;
}
```

可以理解成页面的颜色、布局、字体、边框这些外观设置。

---

### 13.3 JavaScript

网页行为逻辑。

```js
button.onclick = function () {
  alert("clicked");
}
```

可以理解成“让页面动起来的代码”。

---

### 13.4 DOM

浏览器把 HTML 解析后形成的一棵对象树。

你可以先理解成：

“网页在内存中的结构化表示。”

JavaScript 改页面，本质上很多时候就是改 DOM。

---

### 13.5 组件 `component`

现代前端最重要的概念之一。

组件就是一小块可复用的界面和逻辑。

例如：

- 按钮组件
- 导航栏组件
- 用户卡片组件

你可以把它类比成：

“把页面拆成很多小模块，每个模块自己负责一块功能。”

---

### 13.6 `props`

在 React 这类框架里，`props` 是父组件传给子组件的数据。

你可以先把它理解成：

“组件的输入参数。”

---

### 13.7 `state`

组件自己的内部状态。

例如：

- 输入框当前内容
- 计数器当前值
- 是否展开

可以理解成：

“组件自己记住的数据。”

---

### 13.8 事件 `event`

用户操作触发的事情：

- 点击 `click`
- 输入 `change`
- 提交 `submit`

前端代码里大量逻辑都围绕事件展开。

---

### 13.9 API

API 可以广义理解成“别人提供给你调用的功能接口”。

在前端语境里，经常表示：

“向后端请求数据的接口。”

比如：

- 获取用户信息
- 获取商品列表
- 提交登录请求

---

### 13.10 异步 `async`

不是立刻拿到结果，而是要等。

最典型就是网络请求。

---

### 13.11 `await`

等待异步结果：

```js
const data = await fetch("/api/user");
```

可以理解成：

“等这件事做完，再继续往下。”

---

## 14. TypeScript 是什么

前端里你很常见到 TypeScript，简称 TS。

它可以理解成：

“带类型系统的 JavaScript。”

JavaScript 本来类型比较松，TypeScript 会给对象、函数、参数补上更明确的约束。

例如：

```ts
function add(a: number, b: number): number {
  return a + b;
}
```

这和 C 的“参数和返回值都有类型”感觉更接近。

---

### 14.1 TypeScript 里的 `type`

用于给数据起一个类型名字：

```ts
type User = {
  name: string;
  age: number;
};
```

---

### 14.2 TypeScript 里的 `interface`

经常也用来描述对象结构：

```ts
interface User {
  name: string;
  age: number;
}
```

对于入门阅读来说，你可以先把 `type` 和 `interface` 都理解成：

“描述这个数据该长什么样。”

---

## 15. 你看代码时最容易遇到的“像不像 C”

### 15.1 很像的部分

- 变量
- 类型
- 函数
- 条件
- 循环
- `struct`

---

### 15.2 半像半不像的部分

- `class`
- `object`
- `method`
- `constructor`
- `interface`

这些是从“只写过程”走向“组织代码”的概念。

---

### 15.3 和 C 差别较大的部分

- Rust 的所有权
- Go 的接口隐式实现
- 前端组件化
- JavaScript 异步

---

## 16. 一份超短对照表

### 数据组织

- C：`struct`
- Go：`struct`
- Rust：`struct`
- Java/C#：`class`
- TypeScript：`interface` / `type`

### 规定行为

- Java/C#：`interface`
- Go：`interface`
- Rust：`trait`
- 前端 TypeScript：常用于约束对象结构，也可约束方法

### 创建实例

- C：定义变量或手动分配内存
- Java/C#：`new`
- Go：直接创建结构体值
- Rust：结构体字面量或构造函数风格方法

### 错误处理

- C：错误码
- Go：`error`
- Rust：`Result`
- JavaScript：异常或 Promise 拒绝

---

## 17. 看陌生代码的推荐顺序

不要一上来逐行死抠，先看结构。

建议顺序：

1. 看文件名
2. 看它属于哪个模块或包
3. 看有没有 `struct`、`class`、`interface`、`trait`
4. 看主要数据有哪些字段
5. 看主要函数/方法有哪些
6. 看程序从哪里开始调用
7. 最后再看具体逻辑

这样会比逐行硬看轻松很多。

---

## 18. 现阶段最值得先记住的 12 个词

如果你现在不想学太多，先把下面这些吃透：

1. 变量
2. 类型
3. 函数
4. 参数
5. 返回值
6. `struct`
7. `class`
8. `object`
9. `method`
10. `interface`
11. `trait`
12. `JSON`

这 12 个词通了，你看大部分基础示例代码都会顺很多。

---

## 19. 适合你的学习顺序

建议这样补：

1. 先复习变量、函数、参数、返回值
2. 再理解 `struct`
3. 再理解 `class`、`object`、`method`
4. 再看 `constructor` 和 `this`
5. 再看 `public/private`
6. 再看 `interface`
7. 再看 Go 的 `interface`
8. 再看 Rust 的 `trait`、`ownership`
9. 再看 JSON
10. 最后看前端的 `component / props / state`

这个顺序更适合只有 C 基础的人。

---

## 20. 最后的记忆口诀

如果你只想记最关键的几句：

- `struct`：装数据
- `class`：装数据和行为
- `object`：类造出来的具体东西
- `method`：类里的函数
- `constructor`：创建对象时的初始化函数
- `interface`：规定必须提供哪些功能
- `trait`：Rust 里的接口型概念
- `module/package`：组织代码文件
- `JSON`：文本形式的数据结构
- `component`：前端里可复用的一块界面和逻辑

---

## 21. 你下一步最适合做什么

如果你想真正开始“看懂代码”，建议下一步不要继续堆概念，而是做一件更实际的事：

找一段很短的代码，让我按行给你解释。

最适合拿来练手的语言顺序是：

1. Go
2. Java
3. TypeScript
4. Rust

因为：

- Go 比较直
- Java 类和对象很典型
- TypeScript 能带你进入前端
- Rust 最难，放后面更合适

如果你愿意，下一步我可以继续给你做一份：

`用 C 的眼光逐行解释 Go / Rust / TypeScript 示例代码`

那样会比单纯背术语更容易真正看懂。
