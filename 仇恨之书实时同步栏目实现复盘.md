# 仇恨之书实时同步栏目实现复盘

本文按“读完后能看懂、能复写、能排错”的目标整理“仇恨之书”栏目从需求到上线的技术路线。

这个功能的本质不是一个普通静态页面，而是一个带后端数据、实时同步、多人反馈计数的网页小应用。

最终用户看到的是：

- 一个独立的“仇恨之书”网页。
- 顶部大标题、实时总数、副标题和搜索栏。
- 下方四列横向排满的仇恨卡片。
- 右下角匕首形悬浮按钮，点击后弹出书写窗口。
- 每条仇恨可以点“赞同”和“仇恨”，再次点击可以取消。
- 多个浏览器同时打开时，新仇恨和反馈计数会实时同步。

## 1. 一句话理解整体架构

这套功能可以理解成：

```text
浏览器里的仇恨书页面
        ↓ fetch 请求
/api/grudges 后端接口
        ↓ 读写
服务器 JSON 数据文件
        ↓ SSE 广播
所有打开页面的浏览器实时更新
```

更完整一点是：

```text
D:\Code\Xuan-BOMS.github.io\source\grudges
        ↓ Hexo 生成与部署
https://hongworld.online/grudges/
        ↓ 浏览器请求
https://hongworld.online/api/grudges
        ↓ Node.js + Express
/home/xuan/magic-journal-api/server.js
        ↓ JSON 文件持久化
/home/xuan/magic-journal-api/data/journal-store.json
```

关键点：网页文件和后端服务不是同一种东西。

网页文件是静态资源，放在 Hexo 网站里；后端 API 是长期运行的 Node.js 服务，负责保存数据和实时广播。

## 2. 这次新增了哪些能力

### 2.1 独立栏目入口

“仇恨之书”作为一个独立页面放在：

```text
D:\Code\Xuan-BOMS.github.io\source\grudges
```

页面入口是：

```text
D:\Code\Xuan-BOMS.github.io\source\grudges\index.html
```

它没有套用 Hexo 主题模板，只是一个很薄的 HTML 外壳：

```html
<main id="grudge-root"></main>
<script src="/grudges/app.js" defer></script>
```

`#grudge-root` 是前端应用的挂载点。真正的界面由 `app.js` 动态写入这个容器。

### 2.2 搜索和四列展示

主页面包含：

- “仇恨之书”大标题。
- “当代矮人的信息化仇恨管理系统，实时同步 XX 条仇恨”副标题。
- 搜索栏。
- 四列仇恨卡片区。

搜索只在前端执行，不需要额外请求服务器。它会同时搜索：

- 仇恨对象，也就是标题字段。
- 正文。
- 署名。

### 2.3 弹窗书写

书写入口不是占据页面的大表单，而是右下角悬浮按钮。

按钮内容是匕首图标和“记录仇恨”。点击后出现居中的弹窗，填写：

- 仇恨对象，可不写。
- 正文，必填。
- 署名，可不写。

### 2.4 两种反馈

每条仇恨有两个可点击反馈：

- `ale`：显示为啤酒杯，中文叫“赞同”。
- `grudge`：显示为匕首，中文叫“仇恨”。

这两个反馈像点赞一样计数，但支持再次点击取消。

### 2.5 实时同步

别人新增仇恨、点击反馈、取消反馈后，当前页面会自动收到更新。

核心技术是 SSE。

SSE 的英文全称是 Server-Sent Events，意思是“服务器主动发送事件”。浏览器和服务器保持一条长连接，服务器有新数据时推给浏览器。

## 3. 文件职责

### 3.1 前端 HTML

路径：

```text
D:\Code\Xuan-BOMS.github.io\source\grudges\index.html
```

职责很简单：

- 设置中文页面。
- 引入 CSS。
- 提供根节点 `#grudge-root`。
- 引入 JS。

它不负责写具体 UI。

### 3.2 前端逻辑

路径：

```text
D:\Code\Xuan-BOMS.github.io\source\grudges\app.js
```

职责：

- 创建页面 DOM。
- 保存前端状态。
- 请求后端接口。
- 搜索过滤。
- 提交新仇恨。
- 处理赞同和仇恨反馈。
- 连接 SSE 实时同步。
- 在 SSE 不可用时用轮询兜底。

### 3.3 页面样式

路径：

```text
D:\Code\Xuan-BOMS.github.io\source\grudges\styles.css
```

职责：

- 纸页背景。
- 顶部黑色书页压条。
- 左侧红色书签。
- 搜索栏。
- 四列卡片。
- 悬浮匕首按钮。
- 居中弹窗。
- 移动端响应式布局。

### 3.4 后端服务

路径：

```text
D:\Code\Xuan-BOMS.github.io\server.js
```

线上对应：

```text
/home/xuan/magic-journal-api/server.js
```

职责：

- 提供 `/api/grudges` 接口。
- 保存仇恨数据。
- 保存反馈投票人。
- 广播实时更新。
- 与原有 journal 投稿系统共用一个 JSON 存储文件。

### 3.5 测试脚本

相关测试包括：

```text
D:\Code\Xuan-BOMS.github.io\tests\verify-grudges-mvp.js
D:\Code\Xuan-BOMS.github.io\tests\verify-grudges-api.js
D:\Code\Xuan-BOMS.github.io\tests\verify-journal-mvp.js
D:\Code\Xuan-BOMS.github.io\tests\verify-journal-api.js
```

其中：

- `verify-grudges-mvp.js` 检查前端静态文件是否满足功能契约。
- `verify-grudges-api.js` 检查后端 API 行为。
- journal 相关测试用于防止改仇恨功能时破坏原投稿功能。

## 4. 前端状态模型

`app.js` 里有一个集中状态对象：

```js
const state = {
  entries: [],
  query: "",
  busy: false,
  source: null,
  pollTimer: null,
  pendingVotes: new Set(),
  voted: readVoted()
};
```

逐个解释：

- `entries`：当前页面拿到的全部仇恨记录。
- `query`：搜索框中的关键词。
- `busy`：正在提交表单时防止重复提交。
- `source`：SSE 连接对象。
- `pollTimer`：轮询定时器。
- `pendingVotes`：正在发送中的反馈请求，防止连续狂点造成重复请求。
- `voted`：当前浏览器已经点过哪些反馈，来自 `localStorage`。

这里的 State 是英文术语，中文可以理解为“状态”。前端页面不是直接从 HTML 里读最终结果，而是先维护状态，再根据状态重新渲染页面。

## 5. 前端渲染流程

页面启动时，`app.js` 做了三件事：

```js
loadGrudges().catch(error => showMessage(error.message || "同步失败。", true));
connectRealtime();
```

含义是：

1. 先请求一次后端，拿到已有仇恨。
2. 建立实时同步连接。
3. 每次数据变化都调用 `render()` 重新画页面。

`render()` 的核心是：

```js
function render() {
  const visible = filterGrudges(state.entries, state.query);
  countEl.textContent = String(state.entries.length);
  totalCountEl.textContent = String(state.entries.length);
  emptyEl.hidden = visible.length > 0;
  ledgerEl.innerHTML = visible.map(renderCard).join("");
}
```

这段代码说明了一个重要前端思路：

```text
数据 state.entries
        ↓ 搜索过滤
visible
        ↓ renderCard 转换成 HTML 字符串
ledgerEl.innerHTML
```

Render 是英文术语，中文意思是“渲染”，也就是把数据变成用户能看到的界面。

DOM 是 Document Object Model 的缩写，中文可以理解为“浏览器中的页面节点树”。`ledgerEl.innerHTML = ...` 就是在修改 DOM。

## 6. 搜索为什么不用后端接口

搜索函数是：

```js
function filterGrudges(entries, query) {
  const keyword = query.trim().toLowerCase();
  if (!keyword) return entries;
  return entries.filter(item => [item.title, item.body, item.signature]
    .join("\n")
    .toLowerCase()
    .includes(keyword));
}
```

因为仇恨记录量暂时不大，所以最简单可靠的做法是：

```text
一次拿到所有记录
        ↓
浏览器本地按关键词过滤
```

这样搜索体验很快，也不需要新增 `GET /api/grudges?q=...` 这种后端接口。

以后如果数据量变成几万条，再考虑后端分页和服务端搜索。

## 7. 书写新仇恨的流程

用户在弹窗里提交表单后，前端会调用：

```text
POST /api/grudges
```

请求体类似：

```json
{
  "title": "某男性冒险家",
  "body": "这里是正文",
  "signature": "匿名矮人"
}
```

前端代码会先检查正文：

```js
const body = String(formData.get("body") || "").trim();
if (!body) {
  showMessage("正文不能为空。", true);
  return;
}
```

标题和署名可以为空，正文必须存在。

提交成功后：

```text
前端清空表单
        ↓
关闭弹窗
        ↓
主动 loadGrudges 再拉取一次
        ↓
后端也会通过 SSE 广播给所有页面
```

这里有一点冗余是有意的：提交者自己立即刷新一次，其他页面靠 SSE 刷新。

## 8. 后端 API 设计

API 是 Application Programming Interface 的缩写，中文可以理解为“程序之间约定好的调用入口”。

这次新增的后端接口有四个。

### 8.1 获取全部仇恨

```text
GET /api/grudges
```

返回类似：

```json
{
  "entries": [
    {
      "id": "GRD-...",
      "title": "某男性冒险家",
      "body": "正文",
      "signature": "匿名矮人",
      "created_at": "2026-...",
      "updated_at": "2026-...",
      "reactions": {
        "ale": 3,
        "grudge": 5
      }
    }
  ],
  "total": 1,
  "updated_at": "2026-..."
}
```

Endpoint 是英文术语，中文可以理解为“一个具体接口地址”。比如 `GET /api/grudges` 就是一个 endpoint。

### 8.2 实时事件连接

```text
GET /api/grudges/events
```

这个接口不是普通 JSON 返回，而是 SSE 长连接。

后端设置响应头：

```js
response.writeHead(200, {
  "Content-Type": "text/event-stream",
  "Cache-Control": "no-cache, no-transform",
  Connection: "keep-alive"
});
```

`Content-Type: text/event-stream` 告诉浏览器：这是事件流，不是普通网页或普通 JSON。

### 8.3 新增仇恨

```text
POST /api/grudges
```

后端会调用 `normalizeGrudge()` 清洗和限制字段：

```js
function normalizeGrudge(input) {
  const body = cleanText(input.body);
  if (!body) throw httpError("正文不能为空。", 400);
  if (body.length > 2400) throw httpError("正文过长，最多 2400 字。", 400);
  const title = cleanText(input.title).slice(0, 120);
  const signature = cleanText(input.signature).slice(0, 80);
  return {
    id: `GRD-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`,
    title,
    body,
    signature,
    ale_voters: [],
    grudge_voters: [],
    created_at: timestamp,
    updated_at: timestamp
  };
}
```

这里的 normalize 可以理解为“把外部输入清洗成系统内部认可的数据格式”。

### 8.4 点赞同或仇恨

```text
POST /api/grudges/:id/reactions
```

请求体：

```json
{
  "type": "ale",
  "voterId": "dwarf-..."
}
```

`type` 只能是：

```text
ale
grudge
```

后端用这个集合限制：

```js
const GRUDGE_REACTION_TYPES = new Set(["ale", "grudge"]);
```

`Set` 是 JavaScript 里的集合结构，适合表达“只能在这些值里选择”。

## 9. 反馈计数和再次点击取消

最开始反馈更像传统点赞：点一次只会增加。

后来需求改成：

```text
重复点击赞同和仇恨可以取消
```

后端实现的核心逻辑是：

```js
const voterIndex = target[key].indexOf(voterId);
const active = voterIndex < 0;
if (active) {
  target[key].push(voterId);
} else {
  target[key].splice(voterIndex, 1);
}
```

解释：

- 如果投票人不在数组里，说明这次是新增反馈。
- 如果投票人已经在数组里，说明这次是取消反馈。

后端返回：

```js
return { ...publicGrudge(target), viewer_reaction_active: active };
```

`viewer_reaction_active` 表示当前这次点击之后，当前用户对这个按钮是否处于选中状态。

前端收到后同步本地状态：

```js
if (isActive) {
  state.voted.add(voteKey);
} else {
  state.voted.delete(voteKey);
}
writeVoted(state.voted);
```

这样按钮可以正确显示高亮或取消高亮。

## 10. 为什么要用 voterId

需求是“像点赞一样每人只能点一次”。在没有登录系统的情况下，最轻量的做法是给每个浏览器生成一个本地 ID：

```js
function getVoterId() {
  let id = localStorage.getItem(VOTER_KEY);
  if (!id) {
    const random = window.crypto && crypto.randomUUID
      ? crypto.randomUUID()
      : `${Date.now()}-${Math.random().toString(36).slice(2)}`;
    id = `dwarf-${random}`;
    localStorage.setItem(VOTER_KEY, id);
  }
  return id;
}
```

`localStorage` 是浏览器本地存储。它能把字符串保存到当前浏览器里，刷新页面后还在。

这不是强身份认证。用户换浏览器、清缓存、换设备，就会得到新的 `voterId`。

但对当前需求来说，它足够轻量，能实现：

```text
同一个浏览器对同一条仇恨的同一种反馈只能算一次
```

真正的计数权威仍然在后端，因为后端保存了：

```text
ale_voters
grudge_voters
```

前端的 `localStorage` 只是为了让当前浏览器知道按钮要不要显示选中。

## 11. 实时同步是怎样工作的

### 11.1 前端连接 SSE

前端代码：

```js
function connectRealtime() {
  if (!window.EventSource) {
    startPolling();
    return;
  }
  state.source = new EventSource(`${API_ROOT}/events`);
  state.source.addEventListener("grudges", event => {
    applyPayload(JSON.parse(event.data));
  });
  state.source.onerror = () => {
    if (state.source) state.source.close();
    state.source = null;
    startPolling();
  };
}
```

`EventSource` 是浏览器内置的 SSE 客户端对象。它会连接服务器事件流，并在收到事件时触发回调。

如果浏览器不支持 SSE，或者连接断了，就进入 `startPolling()`。

Polling 是英文术语，中文叫“轮询”。意思是每隔一段时间主动请求一次服务器。

当前轮询间隔是：

```js
const POLL_MS = 4000;
```

也就是 4 秒。

### 11.2 后端保存客户端连接

后端有：

```js
const grudgeClients = new Set();
```

每当有浏览器连接 `/api/grudges/events`：

```js
grudgeClients.add(response);
sendGrudgeEvent(response, await store.read());
request.on("close", () => grudgeClients.delete(response));
```

可以把 `grudgeClients` 理解成“所有正在看仇恨之书页面的人”。

### 11.3 后端广播更新

新增仇恨或修改反馈后，后端都会调用：

```js
broadcastGrudges(grudgeClients, store).catch(() => {});
```

广播函数：

```js
async function broadcastGrudges(clients, store) {
  if (!clients.size) return;
  const currentStore = await store.read();
  for (const client of clients) {
    sendGrudgeEvent(client, currentStore);
  }
}
```

这就完成了：

```text
一个用户操作
        ↓
后端修改数据
        ↓
广播给所有浏览器
        ↓
所有页面重新 render
```

## 12. JSON 文件持久化

Persistence 是英文术语，中文叫“持久化”。意思是数据不能只存在内存里，服务重启后也要保留。

这次没有引入数据库，而是复用了原 journal 系统的 JSON 文件：

```text
/home/xuan/magic-journal-api/data/journal-store.json
```

本地开发时由环境变量或默认路径决定：

```js
const dataFile = options.dataFile
  || process.env.JOURNAL_DATA_FILE
  || path.join(root, ".journal-data", "journal-store.json");
```

Environment Variable 是英文术语，中文叫“环境变量”。它是在启动进程时传入的配置，例如：

```bash
PORT=4100 JOURNAL_DATA_FILE=/home/xuan/magic-journal-api/data/journal-store.json node server.js
```

这样代码不用写死线上路径。

## 13. 为什么写文件要用队列和临时文件

后端写数据不是直接随便写，而是通过 `createStoreAccess()`：

```js
function createStoreAccess(dataFile) {
  let queue = Promise.resolve();
  return {
    read: () => readStore(dataFile),
    update(updater) {
      const task = queue.then(async () => {
        const currentStore = await readStore(dataFile);
        const result = await updater(currentStore);
        await writeStore(dataFile, currentStore);
        return result;
      });
      queue = task.catch(() => {});
      return task;
    }
  };
}
```

这里的 queue 是“写入队列”。作用是让多个写操作排队执行，避免两个人同时点击时互相覆盖。

写文件时使用临时文件再重命名：

```js
await fs.writeFile(tempFile, JSON.stringify(...), "utf8");
await fs.rename(tempFile, dataFile);
```

Atomic Write 是英文术语，中文可理解为“原子写入”。这里的思路是：

```text
先把完整新内容写进临时文件
        ↓
写成功后再把临时文件 rename 成正式文件
```

这样比直接覆盖正式文件更安全。即使写到一半失败，也不容易留下半个 JSON 文件。

## 14. 数据模型怎么设计

内部保存的一条仇恨大概是：

```json
{
  "id": "GRD-mabc123-xyz789",
  "title": "某男性冒险家",
  "body": "正文",
  "signature": "匿名矮人",
  "ale_voters": ["dwarf-a", "dwarf-b"],
  "grudge_voters": ["dwarf-c"],
  "created_at": "2026-06-19T10:00:00.000Z",
  "updated_at": "2026-06-19T10:00:00.000Z"
}
```

对前端公开时不返回投票人数组，只返回数量：

```js
function publicGrudge(item) {
  return {
    id: cleanText(item.id),
    title: cleanText(item.title),
    body: cleanText(item.body),
    signature: cleanText(item.signature),
    created_at: cleanText(item.created_at),
    updated_at: cleanText(item.updated_at),
    reactions: {
      ale: Array.isArray(item.ale_voters) ? item.ale_voters.length : 0,
      grudge: Array.isArray(item.grudge_voters) ? item.grudge_voters.length : 0
    }
  };
}
```

这样做有两个好处：

- 前端只需要显示数量，不需要知道每个投票人是谁。
- 不把内部数据结构完整暴露给浏览器。

## 15. UI 设计路线

最开始页面比较像通用模板，后来多次调整为更接近“矮人仇恨之书”的书页感。

最终设计重点：

- 页面不是卡片式后台，而是一张可滚动的旧纸页。
- 上方有黑色压条，像书页顶部的装订和铁件。
- 左侧有红色书签，提示这是书本。
- 主标题居中，搜索栏居中。
- 内容区尽量占满页面，页边距较窄。
- 仇恨记录是纸片式矩形框，不做过重底框。
- 右下角按钮是圆形红色印章感，内部是匕首。
- 反馈按钮不是现代点赞图标，而是啤酒杯和匕首。

这里的关键不是“加很多装饰”，而是让装饰服务主题：

```text
纸张、书页、金属、红皮革、铆钉、匕首、酒杯
```

这些元素共同指向矮人酒馆和仇恨之书，而不是泛用 AI 风网页。

## 16. 四列布局为什么改成 CSS Grid

曾经有一个布局问题：页面排列方式像纵向分栏，不是横着排满一行再进入下一行。

原因通常是使用类似 `column-count` 的布局。它会先从上到下填满第一列，再填第二列。

需求要的是：

```text
第 1 条  第 2 条  第 3 条  第 4 条
第 5 条  第 6 条  第 7 条  第 8 条
```

所以改为 CSS Grid：

```css
.grudge-ledger {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
}
```

CSS Grid 是英文术语，中文叫“CSS 网格布局”。它适合做二维布局，也就是同时控制行和列。

`repeat(4, minmax(0, 1fr))` 的意思是：

- 重复 4 列。
- 每列最小宽度是 0，避免内容把列撑爆。
- 每列平均分配剩余空间。

移动端再通过媒体查询调整：

```css
@media (max-width: 1180px) {
  .grudge-ledger {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}

@media (max-width: 900px) {
  .grudge-ledger {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 560px) {
  .grudge-ledger {
    grid-template-columns: 1fr;
  }
}
```

Responsive 是英文术语，中文叫“响应式”。意思是页面会根据屏幕宽度调整布局。

## 17. 为什么不用 Hexo 模板

用户明确要求参考：

```text
https://hongworld.online/journal/
```

但不要套用 Hexo 模板。

原因是这个页面本质是一个小应用：

- 要有全屏书页设计。
- 要自己控制边距和布局。
- 要有固定右下角按钮。
- 要有居中弹窗。
- 要实时更新。

如果套进 Hexo 主题模板，常见问题是：

- 主题容器会限制宽度。
- 主题导航、页脚和卡片样式会干扰书页风格。
- 独立应用的 CSS 容易被主题 CSS 污染。

所以最终采用独立 HTML：

```text
source/grudges/index.html
source/grudges/app.js
source/grudges/styles.css
```

并通过 `skip_render` 或等效配置让 Hexo 直接复制这些文件。

Hexo 是静态网站生成器。它会把 `source` 里的内容生成到 `public`，再部署到服务器。

## 18. 静态前端和动态后端的部署区别

这次最容易混淆的是：

```text
npm run deploy 只能部署静态网页
```

它会把：

```text
D:\Code\Xuan-BOMS.github.io\source
```

生成并部署到：

```text
/home/www/website
```

但后端 API 不会因为 Hexo deploy 自动更新。

后端线上运行在：

```text
/home/xuan/magic-journal-api/server.js
```

因此后端改动需要单独上传并重启 Node 服务。

这次线上后端启动参数类似：

```bash
PORT=4100 \
JOURNAL_DATA_FILE=/home/xuan/magic-journal-api/data/journal-store.json \
nohup node server.js > journal-api.log 2>&1 &
```

Process 是英文术语，中文叫“进程”。这里的 Node 服务就是一个长期运行的进程。

PID 是 Process ID 的缩写，中文叫“进程编号”。它常被保存到：

```text
journal-api.pid
```

便于后续停止或检查这个服务。

## 19. 从本地到上线的发布路线

### 19.1 前端静态资源

标准流程：

```powershell
cd D:\Code\Xuan-BOMS.github.io
npm run build
npm run deploy
```

这里的 Build 是英文术语，中文叫“构建”。它会把源文件生成浏览器可访问的最终文件。

Deploy 是英文术语，中文叫“部署”。它会把生成结果发布到服务器。

注意：不要直接改：

```text
D:\Code\Xuan-BOMS.github.io\public
```

`public` 是生成结果，下一次构建可能会被覆盖。长期保存的源码应该改：

```text
D:\Code\Xuan-BOMS.github.io\source
```

### 19.2 后端服务

后端发布路线：

```text
本地 server.js
        ↓ scp 上传
/home/xuan/magic-journal-api/server.js
        ↓ node -c 检查语法
重启 Node 进程
        ↓
线上 /api/grudges 生效
```

SSH 是 Secure Shell 的缩写，中文常叫“安全远程登录”。它用于登录服务器执行命令。

SCP 是 Secure Copy 的缩写，中文可以理解为“通过 SSH 安全复制文件”。它用于把本地文件传到服务器。

## 20. 验证路线

这次验证不是只看页面能打开，而是分层验证。

### 20.1 静态契约测试

运行：

```powershell
node tests\verify-grudges-mvp.js
```

目的：

- 检查前端文件存在。
- 检查关键文案存在。
- 检查关键按钮、搜索、弹窗、反馈入口存在。

### 20.2 后端 API 测试

运行：

```powershell
node tests\verify-grudges-api.js
```

目的：

- 新增仇恨可成功。
- 获取列表可成功。
- 反馈计数可成功。
- 重复点击可以取消。

### 20.3 原有投稿功能回归测试

运行：

```powershell
node tests\verify-journal-mvp.js
node tests\verify-journal-api.js
```

Regression Test 是英文术语，中文叫“回归测试”。意思是改新功能后，要确认旧功能没有被破坏。

因为仇恨之书和 journal 投稿系统共用 `server.js` 和 JSON 存储，所以必须跑 journal 测试。

### 20.4 构建验证

运行：

```powershell
npm run build
```

目的：

- 确认 Hexo 可以生成网站。
- 确认静态资源路径正确。
- 确认没有语法错误或构建错误。

### 20.5 浏览器视觉和交互验证

实际验证过：

- 桌面端四列横向排布。
- 移动端变为单列或双列，不溢出。
- 弹窗居中出现。
- 右下角按钮不挡住主要内容。
- “赞同”和“仇恨”文字没有反。
- 反馈按钮不超出卡片。
- 点一次计数加 1，再点一次计数减 1。

这类验证不能只靠代码，因为 UI 是否像设计图、是否太挤、是否有 AI 味，需要直接看页面。

## 21. 如果从零复写，推荐顺序

不要一开始就追求完整 UI。推荐按下面顺序复写。

### 21.1 先做最小页面

创建：

```text
source/grudges/index.html
source/grudges/app.js
source/grudges/styles.css
```

`index.html` 只放：

```html
<main id="grudge-root"></main>
<script src="/grudges/app.js" defer></script>
```

### 21.2 写前端静态假数据

先不要接后端，在 `app.js` 里写：

```js
const state = {
  entries: [
    { title: "某男性冒险家", body: "正文", signature: "匿名矮人", reactions: { ale: 0, grudge: 0 } }
  ],
  query: ""
};
```

然后实现：

- `render()`
- `renderCard()`
- `filterGrudges()`

目标是先让页面能展示和搜索。

### 21.3 做书页 CSS

先完成大结构：

```text
body 纸页背景
grudge-mast 标题区
grudge-search 搜索栏
grudge-ledger 四列网格
grudge-card 仇恨卡片
grudge-fab 右下角按钮
grudge-composer 弹窗
```

不要先堆装饰。先保证：

- 页面可读。
- 宽屏四列。
- 窄屏不溢出。
- 按钮和文字不压住内容。

### 21.4 加后端 GET 接口

在 `server.js` 中先实现：

```text
GET /api/grudges
```

让前端可以：

```js
fetch("/api/grudges")
```

拿到真实数据。

### 21.5 加 POST 新增

实现：

```text
POST /api/grudges
```

后端做字段清洗：

- 正文必填。
- 正文最长 2400。
- 标题最长 120。
- 署名最长 80。

前端表单提交后刷新列表。

### 21.6 加反馈接口

实现：

```text
POST /api/grudges/:id/reactions
```

先做点一次增加，再改为 toggle：

```text
如果 voterId 不存在，就加入数组。
如果 voterId 已存在，就从数组移除。
```

### 21.7 加 SSE 实时同步

后端：

```text
GET /api/grudges/events
```

前端：

```js
new EventSource("/api/grudges/events")
```

每次新增或反馈后，后端广播最新列表。

### 21.8 加轮询兜底

如果 SSE 失败：

```text
每 4 秒 GET /api/grudges
```

这样网络或浏览器不支持 SSE 时，功能仍然可用。

### 21.9 最后做视觉精修

最后再按设计稿精修：

- 窄页边距。
- 书页纹理。
- 顶部黑条。
- 红色书签。
- 四列间距。
- 匕首和酒杯图标。
- 弹窗位置和动画。

先做逻辑，后做风格，能减少返工。

## 22. 常见坑

### 22.1 只部署 Hexo，不会更新后端

`npm run deploy` 只更新静态网站。后端 `server.js` 改了，需要单独上传服务器并重启 Node 服务。

### 22.2 直接改 public 会丢

`public` 是 Hexo 生成目录，不是源码目录。

正确修改位置是：

```text
D:\Code\Xuan-BOMS.github.io\source\grudges
```

### 22.3 column-count 不是横向排满

如果想要：

```text
1 2 3 4
5 6 7 8
```

应该用 CSS Grid，不应该用 `column-count`。

### 22.4 localStorage 不是安全登录

`localStorage` 只能识别当前浏览器。它适合轻量点赞，不适合严肃身份系统。

### 22.5 前端显示不能作为权威

按钮高亮只是当前浏览器显示。真正是否算票，要以后端的 `ale_voters` 和 `grudge_voters` 为准。

### 22.6 SSE 要有降级方案

SSE 连接可能被浏览器、代理或网络中断。用轮询兜底能显著提高可用性。

### 22.7 改共用后端要跑旧功能测试

仇恨之书复用了 journal 服务。如果改 `server.js`，必须检查投稿系统没有被破坏。

### 22.8 UI 图标和文字要实际看

这次就出现过“赞同”和“仇恨”中文反了、按钮超出卡片的问题。代码看起来没报错，不代表视觉正确。

## 23. 英文术语速查

### Frontend

前端。用户在浏览器中看到和操作的部分，例如页面、按钮、搜索框、弹窗、卡片。

### Backend

后端。运行在服务器上的程序，负责保存数据、校验请求、返回接口结果。

### API

程序接口。前端和后端之间约定好的调用方式，例如 `GET /api/grudges`。

### Endpoint

具体接口地址。例如 `POST /api/grudges/:id/reactions` 是一个 endpoint。

### Express

Node.js 的 Web 服务框架。它可以写 `app.get()`、`app.post()` 这样的后端路由。

### Route

路由。服务器根据请求路径和方法选择执行哪段代码，例如 `app.get("/api/grudges", ...)`。

### JSON

一种轻量数据格式。前后端常用它传输对象和数组。

### State

状态。前端保存当前数据和交互信息的对象，例如 `entries`、`query`、`voted`。

### Render

渲染。把数据转换成界面。

### DOM

Document Object Model，文档对象模型。浏览器把 HTML 页面解析成节点树，JavaScript 可以修改这些节点。

### Fetch

浏览器发网络请求的 API。前端用它请求后端数据。

### EventSource

浏览器内置的 SSE 客户端对象，用来接收服务器主动推送的事件。

### SSE

Server-Sent Events，服务器发送事件。服务器通过长连接主动把更新推给浏览器。

### Polling

轮询。客户端每隔一段时间主动请求服务器，作为实时同步的兜底方案。

### localStorage

浏览器本地存储。刷新页面后仍保留，适合保存轻量用户设置或本地标识。

### Voter ID

投票人标识。这里是每个浏览器生成的 `dwarf-...` 字符串，用来避免同一浏览器重复计数。

### CSS Grid

CSS 网格布局。适合做四列卡片这种横向排满再换行的布局。

### Responsive

响应式。页面根据屏幕宽度自动改变布局。

### Modal

模态弹窗。打开后覆盖当前页面，用户先处理弹窗内容。

### FAB

Floating Action Button，悬浮操作按钮。这里就是右下角“记录仇恨”圆形按钮。

### Hexo

静态网站生成器。它把 `source` 目录生成到 `public`，再部署到服务器。

### Build

构建。把源码转换成可发布文件。

### Deploy

部署。把构建结果发布到线上服务器。

### SSH

Secure Shell，安全远程登录。用于连接服务器执行命令。

### SCP

Secure Copy，通过 SSH 复制文件。用于把本地后端文件上传到服务器。

### Process

进程。正在运行的程序实例。Node 后端服务就是一个进程。

### PID

Process ID，进程编号。系统用它识别某个正在运行的进程。

### Environment Variable

环境变量。启动程序时传入的配置，例如端口和数据文件路径。

### Persistence

持久化。把数据保存到文件或数据库里，让服务重启后数据仍然存在。

### Atomic Write

原子写入。先写临时文件，再重命名成正式文件，减少写坏数据的风险。

### Regression Test

回归测试。修改新功能后，检查旧功能是否仍然正常。

## 24. 最小心智模型

记住这四层就能读懂大部分实现：

```text
1. 页面层
   source/grudges/index.html
   source/grudges/app.js
   source/grudges/styles.css

2. 接口层
   GET  /api/grudges
   GET  /api/grudges/events
   POST /api/grudges
   POST /api/grudges/:id/reactions

3. 存储层
   journal-store.json 里的 grudges 数组
   每条记录保存正文、署名、反馈人数组和时间

4. 同步层
   新增或反馈后，后端广播 SSE
   浏览器收到事件后更新 state.entries 并重新 render
```

## 25. 一句话总结

“仇恨之书”是一个嵌入 Hexo 网站的独立前端小应用，前端负责书页 UI、搜索、弹窗和交互，后端 Express API 负责数据校验、JSON 持久化、反馈去重和 SSE 实时广播。理解它的关键是把“静态网页部署”和“动态后端服务”分开看，再沿着 `页面点击 -> fetch/API -> JSON 存储 -> SSE 广播 -> render 更新` 这条链路阅读代码。
