# 轰界法术编译器静态化与网站部署全流程

本文解释“轰界法术编译器”为什么本地工程文件很多，但网站目录里只剩几个文件，以及从本地源码到线上网页的完整转换过程。

目标读完后能回答三件事：

- 哪些文件是网页运行必需的。
- 哪些文件只是构建材料或开发依赖。
- 修改 `zh_cn.json` 后如何重新生成并部署到 `https://hongworld.online/tools/magic-spell-compiler/`。

## 1. 最终网页为什么只有几个文件

线上目录是：

```text
/home/www/website/tools/magic-spell-compiler/
```

本地对应的 Hexo 生成结果目录是：

```text
D:\Code\Xuan-BOMS.github.io\public\tools\magic-spell-compiler
```

当前实际文件只有：

```text
index.html
assets/index-BoufSPAh.js
assets/index-EUvBZEMO.css
```

这不是缺文件，而是前端构建工具已经把大量源码、数据和依赖合并进少数几个浏览器可直接运行的文件。

可以把它理解成：

```text
大量源码、JSON、React 组件、样式、编译逻辑
        ↓ 构建
少数浏览器可运行文件
```

最终网页不需要保留 `src`、`node_modules`、`package.json`、`tsconfig.json` 这些开发材料。浏览器只会读取 HTML、JS、CSS 和被 JS/CSS 引用的静态资源。

## 2. 三个最终文件分别是什么

### 2.1 `index.html`

路径：

```text
D:\Code\Xuan-BOMS.github.io\source\tools\magic-spell-compiler\index.html
D:\Code\Xuan-BOMS.github.io\public\tools\magic-spell-compiler\index.html
/home/www/website/tools/magic-spell-compiler/index.html
```

作用：

- 是浏览器打开页面时读取的入口文件。
- 提供一个空容器 `<div id="root"></div>`。
- 引用真正的 JS 和 CSS。

内容大致是：

```html
<div id="root"></div>
<script type="module" src="/tools/magic-spell-compiler/assets/index-BoufSPAh.js"></script>
<link rel="stylesheet" href="/tools/magic-spell-compiler/assets/index-EUvBZEMO.css">
```

这里的 `root` 是 React 挂载点。React 会把整个法术编译器界面渲染到这个容器里。

### 2.2 `assets/index-BoufSPAh.js`

这是最核心的文件。

它包含：

- React 页面逻辑。
- 四阶段工作台界面。
- 节点抽屉交互。
- 六维雷达图。
- 前端版法术编译逻辑。
- 节点库 JSON。
- 中文文本包 JSON。
- 固定法术 profiles。
- 示例 examples。
- 图标和状态切换逻辑。

也就是说，原本散落在很多 `.tsx`、`.ts`、`.json` 里的东西，在构建后被压缩、合并、转换成这个 JS 文件。

### 2.3 `assets/index-EUvBZEMO.css`

这是页面样式文件。

它包含：

- 页面布局。
- 顶部栏样式。
- 节点抽屉样式。
- 四阶段卡片样式。
- 按钮、状态、标签样式。
- 六维雷达图样式。
- 移动端响应式规则。

## 3. 特殊名词解释

### 3.1 静态网站

静态网站是指服务器只负责发送现成文件，例如 HTML、JS、CSS、图片。

访问时不需要服务器运行后端 Python，也不需要实时调用数据库。

当前法术编译器上线后就是静态网页：所有编译逻辑都在浏览器里的 JS 中执行。

### 3.2 前端

前端就是用户在浏览器里看到和操作的部分。

本项目的前端包括：

- 页面按钮。
- 节点抽屉。
- 四阶段构建区。
- 编译结果。
- 雷达图。

### 3.3 React

React 是一个前端界面库。

它负责根据数据状态生成页面。比如用户选择一个节点后，React 会重新渲染阶段栏、结果区、按钮状态。

### 3.4 TypeScript

TypeScript 是带类型检查的 JavaScript。

开发时写 `.ts` 或 `.tsx`，构建后会转换成浏览器可以运行的 JavaScript。

### 3.5 Vite

Vite 是前端构建工具。

它负责把源码转换成最终发布文件。

本项目里执行：

```powershell
npm run build
```

实际做的是：

```text
tsc && vite build
```

其中：

- `tsc` 做 TypeScript 类型检查。
- `vite build` 生成最终静态文件。

### 3.6 打包

打包是指把很多源码文件合并、压缩、转换成少量发布文件。

例子：

```text
src/main.tsx
src/staticCompiler.ts
src/data/texts.json
src/data/nodes.json
React 依赖
lucide 图标
        ↓ 打包
assets/index-BoufSPAh.js
```

### 3.7 Hash

`index-BoufSPAh.js` 里的 `BoufSPAh` 是 hash，也可以理解为内容指纹。

文件内容变了，hash 通常也会变。

这样做的好处是浏览器不会误用旧缓存。

例如：

```text
旧版本：index-UO5d9gpY.js
新版本：index-BoufSPAh.js
```

当 `index.html` 引用新的 JS 文件名时，浏览器会下载新版本。

### 3.8 `node_modules`

`node_modules` 是 npm 下载的依赖库目录。

开发和构建需要它，但网页运行不需要它。

不要把 `node_modules` 放进网站发布目录。

### 3.9 `dist`

`dist` 是 Vite 构建输出目录。

它是“构建后的成品目录”。

当前流程里：

```text
D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static\dist
```

会被复制到：

```text
D:\Code\Xuan-BOMS.github.io\source\tools\magic-spell-compiler
```

### 3.10 Hexo

Hexo 是静态网站生成器。

它把：

```text
D:\Code\Xuan-BOMS.github.io\source
```

生成到：

```text
D:\Code\Xuan-BOMS.github.io\public
```

然后 `hexo deploy` 把 `public` 里的内容推到服务器。

### 3.11 `source`

`source` 是 Hexo 的源内容目录。

站点源码里需要长期保存的网页资源放这里。

法术编译器最终静态包放在：

```text
D:\Code\Xuan-BOMS.github.io\source\tools\magic-spell-compiler
```

### 3.12 `public`

`public` 是 Hexo 生成后的结果目录。

它不是手写源码目录。

执行：

```powershell
hexo clean
```

会删除 `public`。

所以不要只改 `public`，因为下一次生成会丢失。

### 3.13 `skip_render`

`skip_render` 是 Hexo 配置项。

本项目里有：

```yaml
skip_render:
  - tools/magic-spell-compiler/**
```

意思是：

```text
source/tools/magic-spell-compiler/ 下的文件不要被 Hexo 当作 Markdown 或模板处理，直接复制到 public。
```

这很重要。

否则 Hexo 可能把 React 的 `index.html` 包进主题模板，导致应用无法作为独立页面正常运行。

### 3.14 裸 Git 仓库

服务器上：

```text
/home/git/blog.git
```

是裸 Git 仓库。

裸仓库没有普通工作目录，主要用于接收 `git push`。

Hexo deploy 会把生成结果推送到这个仓库。

### 3.15 `post-receive` 钩子

`post-receive` 是 Git 服务器收到推送后自动执行的脚本。

当前服务器钩子内容等价于：

```bash
git --work-tree=/home/www/website --git-dir=/home/git/blog.git checkout -f
```

意思是：

```text
收到 Hexo 推送后，把仓库内容强制 checkout 到 /home/www/website。
```

### 3.16 Nginx 网站根目录

服务器真正对外提供网页的目录是：

```text
/home/www/website
```

当浏览器访问：

```text
https://hongworld.online/tools/magic-spell-compiler/
```

服务器实际读取：

```text
/home/www/website/tools/magic-spell-compiler/index.html
```

## 4. 本项目的几个目录各自负责什么

### 4.1 原始法术项目

路径：

```text
D:\magic\creat-magic
```

这是法术编译器原项目。

里面有：

```text
backend/
frontend/
data/
```

其中当前最常改的是：

```text
D:\magic\creat-magic\data\texts\zh_cn.json
```

它保存中文文本，包括：

- UI 文案。
- 风险文本。
- 雷达标签。
- 固定法术名称。
- 固定法术简介。

### 4.2 静态编译器构建工作区

路径：

```text
D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static
```

这是把原项目改造成“纯静态前端应用”的工作区。

主要文件：

```text
src/main.tsx
src/staticCompiler.ts
src/styles.css
src/staticData.ts
src/types.ts
src/data/nodes.json
src/data/texts.json
src/data/profiles.json
src/data/examples.json
package.json
vite.config.ts
tsconfig.json
```

这些是构建网页需要的材料。

它们不会直接出现在网站最终目录里。

### 4.3 Vite 输出目录

路径：

```text
D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static\dist
```

执行：

```powershell
npm run build
```

后生成。

这里的内容就是准备复制到 Hexo 站点的静态成品。

### 4.4 Hexo 站点源码目录

路径：

```text
D:\Code\Xuan-BOMS.github.io
```

这里是整个网站工程。

法术编译器在站点里的长期保存位置是：

```text
D:\Code\Xuan-BOMS.github.io\source\tools\magic-spell-compiler
```

工具入口页是：

```text
D:\Code\Xuan-BOMS.github.io\source\tools\index.md
```

Hexo 配置是：

```text
D:\Code\Xuan-BOMS.github.io\_config.yml
```

部署说明是：

```text
D:\Code\Xuan-BOMS.github.io\DEPLOYMENT.md
```

### 4.5 Hexo 生成目录

路径：

```text
D:\Code\Xuan-BOMS.github.io\public
```

执行：

```powershell
hexo generate
```

后生成。

这个目录是本地准备部署到服务器的完整网站。

不要把它当作主要编辑入口。

## 5. 完整转换链路

整体链路如下：

```text
D:\magic\creat-magic\data\texts\zh_cn.json
        ↓ 同步文本
D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static\src\data\texts.json
        ↓ 由后端 profile 逻辑重新生成
D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static\src\data\profiles.json
D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static\src\data\examples.json
        ↓ npm run build
D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static\dist
        ↓ 复制构建结果
D:\Code\Xuan-BOMS.github.io\source\tools\magic-spell-compiler
        ↓ hexo generate
D:\Code\Xuan-BOMS.github.io\public\tools\magic-spell-compiler
        ↓ hexo deploy
/home/git/blog.git
        ↓ post-receive checkout
/home/www/website/tools/magic-spell-compiler
        ↓ Nginx 对外提供
https://hongworld.online/tools/magic-spell-compiler/
```

## 6. 为什么 `profiles.json` 和 `examples.json` 也要更新

只改 `texts.json` 不一定够。

原因是静态应用里有三份和固定法术名称有关的数据：

```text
texts.json
profiles.json
examples.json
```

它们职责不同。

`texts.json`：

```text
集中中文文本包。
```

`profiles.json`：

```text
固定法术签名表。
```

它告诉前端：

```text
某一组四阶段核心节点组合应该识别成哪个固定法术。
```

`examples.json`：

```text
示例下拉框数据。
```

它决定示例列表里显示什么名字，比如：

```text
4阶 疾行术
1阶 石弹术
```

所以修改固定法术名称后，应该重新生成 `profiles.json` 和 `examples.json`。

## 7. 哪些文件是运行必需的

对网站运行而言，当前必需的是：

```text
D:\Code\Xuan-BOMS.github.io\source\tools\magic-spell-compiler\index.html
D:\Code\Xuan-BOMS.github.io\source\tools\magic-spell-compiler\assets\index-BoufSPAh.js
D:\Code\Xuan-BOMS.github.io\source\tools\magic-spell-compiler\assets\index-EUvBZEMO.css
```

以及入口和配置：

```text
D:\Code\Xuan-BOMS.github.io\source\tools\index.md
D:\Code\Xuan-BOMS.github.io\_config.yml
```

如果以后页面引入图片、字体、音频等资源，`assets` 里可能会多出其他文件。判断标准是：

```text
index.html、JS、CSS 中被引用到的资源都必须保留。
```

## 8. 哪些文件不是线上运行必需的

这些不需要放进最终网页目录：

```text
src/
node_modules/
package.json
package-lock.json
tsconfig.json
vite.config.ts
backend/
frontend/
data/*.json
```

它们不是“无用”，而是“构建时有用，运行时不需要”。

更准确地说：

```text
开发材料：需要保留在开发环境中。
发布成品：只需要放到网站目录和服务器。
```

不要把开发材料直接复制到 `public/tools/magic-spell-compiler`。

## 9. 修改文本并部署的标准流程

### 9.1 修改文本

编辑：

```text
D:\magic\creat-magic\data\texts\zh_cn.json
```

修改后先检查 JSON 是否有效：

```powershell
cd D:\magic\creat-magic
Get-Content .\data\texts\zh_cn.json -Raw -Encoding UTF8 | ConvertFrom-Json | Out-Null
```

如果命令无报错，说明 JSON 格式有效。

### 9.2 同步文本到静态构建工作区

```powershell
cd D:\magic\creat-magic

$static = "D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static"
Copy-Item ".\data\texts\zh_cn.json" "$static\src\data\texts.json" -Force
```

### 9.3 重新生成固定法术和示例数据

```powershell
$env:PYTHONPATH = "D:\magic\creat-magic\backend"

@'
import json
from dataclasses import asdict
from pathlib import Path
from app.fixed_spell_profiles import SPELL_PROFILES, _with_external_text, get_fixed_spell_examples

out = Path(r"D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static\src\data")
profiles = [asdict(_with_external_text(profile)) for profile in SPELL_PROFILES]
examples = [example.model_dump() for example in get_fixed_spell_examples()]

(out / "profiles.json").write_text(json.dumps(profiles, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
(out / "examples.json").write_text(json.dumps(examples, ensure_ascii=False, indent=2) + "\n", encoding="utf-8")
'@ | python -
```

这一步保证固定法术识别结果和下拉示例名称都同步更新。

### 9.4 重新构建静态应用

```powershell
cd D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static
npm run build
```

成功后会生成：

```text
D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static\dist
```

### 9.5 复制构建结果到 Hexo source

```powershell
$static = "D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static"
$target = "D:\Code\Xuan-BOMS.github.io\source\tools\magic-spell-compiler"

Remove-Item "$target\*" -Recurse -Force
Copy-Item "$static\dist\*" $target -Recurse -Force
```

注意：旧 hash 的 JS 文件要删除。

例如旧文件：

```text
index-UO5d9gpY.js
```

新文件：

```text
index-BoufSPAh.js
```

如果旧文件不删，虽然通常不影响当前 `index.html`，但会让目录变脏，长期难以判断哪个才是正在使用的文件。

### 9.6 生成并部署 Hexo 网站

```powershell
cd D:\Code\Xuan-BOMS.github.io
hexo clean
hexo generate
hexo deploy
```

这三步含义分别是：

```text
hexo clean     删除旧 public 和缓存。
hexo generate 重新生成 public。
hexo deploy   把 public 推送到服务器部署仓库。
```

### 9.7 检查 Git 状态

```powershell
cd D:\Code\Xuan-BOMS.github.io
git status --short
```

如果只是更新法术编译器，通常应该看到：

```text
D source/tools/magic-spell-compiler/assets/旧文件.js
M source/tools/magic-spell-compiler/index.html
?? source/tools/magic-spell-compiler/assets/新文件.js
```

如果看到：

```text
M source/vedio/subtitles.json
```

要谨慎。

Hexo 流程可能会自动重写字幕文件时间戳。若本次不是更新字幕，不要把它作为有效改动提交。

## 10. 部署到服务器的机制

网站 Hexo 配置里有：

```yaml
deploy:
  type: git
  repo: ssh://git@154.219.106.185:2222/home/git/blog.git
  branch: master
```

执行：

```powershell
hexo deploy
```

会把 `public` 目录推送到：

```text
/home/git/blog.git
```

服务器的 Git 钩子再把它发布到：

```text
/home/www/website
```

最终浏览器访问的是：

```text
https://hongworld.online/
```

对应服务器目录：

```text
/home/www/website
```

## 11. 服务器权限为什么会影响 deploy

`hexo deploy` 使用的是服务器上的 `git` 用户。

如果网站目录属于其他用户，并且 `git` 用户没有写权限，就会出现：

```text
unable to unlink old ...
权限不够
```

正确状态应该类似：

```text
/home/www/website  git:git
```

修复命令是：

```bash
sudo chown -R git:git /home/www/website
sudo chmod 755 /home/www /home/www/website
sudo find /home/www/website -type d -exec chmod 755 {} +
sudo find /home/www/website -type f -exec chmod 644 {} +
```

不要把 sudo 密码写进仓库或文档。

## 12. 如何确认线上已经更新

### 12.1 检查页面入口

打开：

```text
https://hongworld.online/tools/
```

应该能看到：

```text
轰界法术编译器
```

### 12.2 检查编译器页面

打开：

```text
https://hongworld.online/tools/magic-spell-compiler/
```

应该能看到：

- 轰界法术生成器。
- 四阶段：开模、提纯、灌注、释放。
- 默认火球术结果。
- 六维雷达图。
- 修改后的固定法术名称。

### 12.3 检查是否加载新 JS

查看 `index.html` 应该引用最新 hash：

```html
<script type="module" src="/tools/magic-spell-compiler/assets/index-BoufSPAh.js"></script>
```

如果线上仍引用旧 JS，可能是：

- `dist` 没重新复制到 `source`。
- `hexo generate` 没重新执行。
- `hexo deploy` 没成功。
- 浏览器缓存了旧页面。

可以给 URL 加查询参数强制刷新：

```text
https://hongworld.online/tools/magic-spell-compiler/?v=check
```

## 13. `public` 目录为什么不能作为编辑入口

`public` 是生成结果。

执行：

```powershell
hexo clean
```

会删除它。

如果直接修改：

```text
D:\Code\Xuan-BOMS.github.io\public\tools\magic-spell-compiler
```

下一次生成就会丢失。

正确做法是修改：

```text
D:\Code\Xuan-BOMS.github.io\source\tools\magic-spell-compiler
```

然后让 Hexo 复制到 `public`。

## 14. 一句话理解整套系统

这套部署不是把整个开发工程搬到服务器上运行。

它是：

```text
把法术编译器预先构建成浏览器可直接运行的静态文件，
再把这些静态文件作为 Hexo 网站的一个工具页面发布。
```

所以服务器不需要 Python 后端，也不需要启动 Vite 开发服务器。

用户访问网页时，浏览器下载 `index.html`、JS、CSS，然后在本地浏览器里运行法术编译器。

## 15. 最小心智模型

可以记成四层：

```text
1. 原始内容层
   D:\magic\creat-magic\data\texts\zh_cn.json

2. 静态应用构建层
   D:\magic\creat-magic\.vibeskills\tmp\magic-compiler-static

3. Hexo 网站源码层
   D:\Code\Xuan-BOMS.github.io\source\tools\magic-spell-compiler

4. 线上发布层
   /home/www/website/tools/magic-spell-compiler
```

每次更新时，不要跳层手改最终结果。

推荐顺序永远是：

```text
改原始内容
同步静态数据
Vite build
复制 dist 到 Hexo source
Hexo generate
Hexo deploy
线上验证
```

