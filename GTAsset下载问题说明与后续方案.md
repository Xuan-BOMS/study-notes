# GTAsset 下载问题说明与后续方案

## 先说结论

之前不是你电脑有问题，而是我采用的下载方式太激进了。

我当时为了加快速度，把任务拆成了多路并行。  
每一路都会再启动一个无头浏览器。  
结果就是：

- 任务管理器里会出现很多 `node.exe`
- 还会出现更多 `chrome-headless-shell.exe`
- 长时间跑下去以后，网页本身也会变得不稳定

所以你看到的现象是：

- 进程越来越多
- CPU 被吃得很高
- 有时任务又会自己停掉

这不是单一问题，而是几件事情叠在一起导致的。

## 到底哪里出问题了

### 1. 并行太多，浏览器子进程也跟着变多

我原来开的不是一个下载任务，而是多路并行。

每一路下载任务本身只是一个 `node` 进程，但它后面还会再带出一整组浏览器子进程。  
所以表面上看，不是 4 个任务，而像是十几个甚至更多进程一起在跑。

这就是为什么你看截图会觉得“明显开多了”。

### 2. 网站页面本身不适合长时间高强度截图

这个网站是 WebGL 渲染。  
我这边日志里已经看到明显证据：

- 页面截图时出现 GPU stall
- 页面上下文被关闭
- 页面直接 crash

意思是：  
不是只有脚本逻辑会出错，网站本身在长时间、大量截图的情况下也会越来越不稳定。

### 3. 我之前的脚本里还有几处放大问题的实现方式

我后来已经定位到几个关键点：

- 切到 `character` 后，只等固定时间，不够稳  
  结果有时会误读成 `illust`
- 我曾经手动调用页面里的 `render()`  
  这会触发页面内部对象没准备好的报错
- 浏览器挂掉时，恢复策略不够保守  
  有时 page 死了，整条任务也跟着废掉

这些问题会让“本来就不稳定的长时间批量截图”变得更容易崩。

## 目前已经修了什么

这些我已经修过：

- 不再误切到 `illust`
- 去掉了手动 `render()` 调用
- 浏览器挂掉时，恢复逻辑更稳
- 正式输出目录不再暴露空文件夹和半成品目录

也就是说，之前最明显的几类脚本 bug 已经处理过了。

## 为什么我现在不继续用原来的方式

因为原来的方式虽然快一点，但风险太高：

- 进程太多
- CPU 压力大
- 跑久了容易崩
- 一崩就要重新收拾残局

你已经明确反馈“看不懂但感觉不对”，这说明我不能再继续用那种高负载方案硬跑。

## 我现在准备怎么改

接下来我会改成更保守的方式：

### 新方案

1. 不再多路并行跑很多 shard
2. 一次只跑一个小批次
3. 每批只处理少量角色
4. 每批结束后主动退出
5. 下一批再重新启动一个干净环境

你可以把它理解成：

- 旧方案：一辆车一直高速跑到底，跑久了容易爆胎
- 新方案：每跑一小段就进站检查，然后再继续

这样做的代价是：

- 速度会慢一些

但换来的好处是：

- 进程少很多
- CPU 压力更低
- 更不容易出现整批任务失控
- 即使某一小批失败，也只影响这一小批，不会把整轮任务拖死

## 你最关心的事：会不会再出现一堆进程

按新方案，下载相关进程数量会明显减少。

不会再像之前那样同时挂很多路后台下载。  
我会采用“单任务、短批次、自动续跑”的方式。

这不能保证网站本身 100% 永远不崩，  
但可以保证：

- 即使崩，也只影响当前这一个小批次
- 不会再堆出一堆你关不过来的下载进程
- 失败后也更容易续跑

## 现在的最终判断

这次问题的本质不是一条代码错了，而是：

- 原执行策略过于激进
- 网站本身长期截图不稳定
- 脚本里又有几处会放大风险的细节

我现在已经换思路，不再追求“多开提速”，而是改成“少开、稳跑、批次回收”。

这才是更符合你机器实际情况、也更不容易再次出问题的方案。
