# 混沌棋项目架构与实现原理

本文按“读懂后可以自己写出来”的目标整理。它不只列文件，还说明每层为什么这样分、数据怎样流动、一次操作从点击到结算怎样完成。

## 1. 项目整体定位

这是一个 Godot 4.6 项目，核心是一个本地双人同步回合制棋类游戏。

项目目前没有复杂场景树，也没有多场景切换。主场景只有一个 `Control` 根节点，挂载主脚本 `scripts/app/game_app.gd`。所有界面，包括主菜单、设置页、教程页、对局页、暂停页，都由这个脚本运行时创建 UI 节点。

规则层独立放在 `scripts/rules/chaos_rules.gd`。它不依赖 UI，负责保存棋局状态、判断合法操作、同步结算、处理冲突、判定胜负。UI 层只负责展示状态、收集玩家点击，然后把“意图”交给规则层。

最核心的分层是：

```text
project.godot
  -> scenes/main.tscn
    -> scripts/app/game_app.gd        UI、流程、输入、教程关卡
      -> scripts/rules/chaos_rules.gd 规则、状态、合法性、结算
        -> assets/pixel/...           棋子、棋盘、按钮、图标素材
```

## 2. 目录结构怎么理解

### 2.1 运行时代码

`scenes/main.tscn`

这是游戏入口场景。它只定义一个名为 `ChaosApp` 的 `Control` 节点，并绑定 `game_app.gd`。这样做的结果是：场景文件很薄，真正的界面结构在代码里动态生成。

`scripts/app/game_app.gd`

应用层脚本。职责包括：

- 加载像素素材到 `textures` 字典。
- 创建主菜单、设置页、教程页、联机占位页、对局页、暂停页。
- 渲染棋盘、侧栏、行动按钮、日志。
- 处理玩家点击棋盘格和按钮。
- 生成规则层需要的行动意图。
- 创建教程预设局面。

`scripts/rules/chaos_rules.gd`

规则层脚本。职责包括：

- 创建棋局状态。
- 表达棋子数据。
- 判断转化、激发、合成、拆分、还原是否合法。
- 找出可选目标格。
- 接收行动意图并消耗 AP。
- 在双方同序号行动都准备好时同步结算。
- 处理冲突、土防御、火扩散、胜利。

### 2.2 资源与生成脚本

`assets/pixel`

Godot 实际运行时加载的素材。包括：

- `board`：棋盘格、选中格、可行动格、冲突格、棋盘框。
- `pieces`：白棋、四元素棋、高阶棋、复合棋、以太棋、混沌棋。
- `ui`：按钮、面板、图标。

`plan/assets`

规划与生成资产的备份目录。它不是运行时加载目录。

`plan/scripts/generate_pixel_assets.py`

用 Pillow 程序化生成像素美术资产。它定义颜色表、棋子绘制函数、棋盘格绘制函数、按钮和图标绘制函数。生成逻辑是确定性的，适合反复调整颜色和形状后重新生成。

### 2.3 测试与输出

`scripts/tests/rules_smoke_test.gd`

直接测试规则层，不启动完整 UI。覆盖首手转化、元素效果、合成、拆分、混沌等核心行为。

`scripts/tests/app_flow_smoke_test.gd`

实例化主场景，测试 UI 流程和按钮可用性。它会检查主菜单、设置、开始游戏、教程练习、复合棋贴图映射等。

`scripts/tests/app_screenshot_test.gd`

用 Godot 自身渲染截图，检查所有可见 `Control` 没有超出 1280x720 视口，并保存主菜单、教程、对局等截图到 `outputs/test_screenshots`。

`outputs`

运行输出目录，通常不提交。截图测试会写入这里。

## 3. Godot 入口流程

Godot 启动后读取 `project.godot`：

```text
run/main_scene = res://scenes/main.tscn
viewport = 1280 x 720
stretch = canvas_items / expand
texture_filter = 0
```

`texture_filter = 0` 对像素风很重要，表示纹理默认不做平滑过滤，棋子和 UI 保持清晰像素边缘。

主场景实例化后，`ChaosApp._ready()` 执行：

```gdscript
func _ready() -> void:
	randomize()
	_load_textures()
	_build_shell()
	show_main_menu()
```

可以把它理解为四步：

1. 初始化随机数，用于同步冲突随机裁决。
2. 把所有 PNG 资源加载到 `textures` 字典。
3. 创建全屏根容器 `root_panel`。
4. 显示主菜单。

## 4. UI 层实现原理

`game_app.gd` 没有依赖编辑器里摆好的复杂节点，而是用代码创建界面。

### 4.1 基础界面容器

`_build_shell()` 创建一个全屏 `MarginContainer`：

```text
ChaosApp(Control)
  -> root_panel(MarginContainer)
    -> screen(VBoxContainer，每次切页重建)
```

切换页面时调用 `_clear_screen()`：

- 删除旧的 `screen`。
- 新建一个 `VBoxContainer`。
- 清空选中格和目标选择模式。

所以主菜单、设置页、教程页、对局页不是多个场景，而是同一个根节点下不断重建子节点。

### 4.2 页面函数

主要页面函数：

- `show_main_menu()`：标题和四个按钮：开始游戏、教程、联机对战、退出。
- `show_setup()`：棋盘尺寸选择、测试模式、开始对局。
- `show_tutorial()`：教程说明和 8 个练习入口。
- `show_online()`：联机占位入口。
- `_show_game()`：真实对局界面。
- `_show_pause()`：暂停页。

按钮统一通过 `_make_button()` 和 `_make_action_button()` 创建。这样按钮尺寸、图标、九宫格背景、危险按钮样式都集中管理。

### 4.3 对局界面布局

对局页由 `_show_game()` 构建，大体是：

```text
screen
  -> header
     -> 状态文本
     -> 暂停按钮
     -> 教程返回按钮（教程模式才有）
  -> body
     -> left_panel   选中信息、棋子效果、日志
     -> center_zone  顶部提示、棋盘、底部提示
     -> side_panel   操作标题、行动按钮
```

棋盘是 `GridContainer`，列数等于 `state.size`。每个格子是一个 `Button`，图标是棋子贴图，背景是棋盘格贴图。

这种设计有一个优点：棋盘格天然可点击，不需要自己写鼠标坐标到格子的换算。

## 5. 规则层状态模型

规则层用一个 `Dictionary` 保存完整棋局状态。`ChaosRules.new_state(size)` 返回类似结构：

```gdscript
{
	"size": 5,
	"board": board,
	"planner": 1,
	"round": 1,
	"ap_budget": {1: 1, 2: 1},
	"ap_used": {1: 0, 2: 0},
	"staged": {1: {}, 2: {}},
	"sieges": {},
	"winner": 0,
	"winner_reason": "",
	"log": [...]
}
```

字段含义：

- `size`：棋盘边长。
- `board`：二维数组，`board[y][x]` 是一个棋子字典。
- `planner`：当前正在规划行动的玩家。
- `round`：当前回合数。
- `ap_budget`：本回合每个玩家可用行动次数。
- `ap_used`：本回合每个玩家已用行动次数。
- `staged`：双方当前同序号待结算指令。
- `sieges`：土防御累计进度。
- `winner` / `winner_reason`：胜负状态。
- `log`：给 UI 展示的日志。

棋子也是 `Dictionary`：

```gdscript
{
	"kind": "fire",
	"owner": 1,
	"tier": 1,
	"slots": []
}
```

字段含义：

- `kind`：棋子类型，如 `white`、`fire`、`composite`、`ether`、`chaos`。
- `owner`：0 表示无主，1/2 表示玩家。
- `tier`：基础棋为 1，高阶棋为 2。
- `slots`：复合棋保存元素槽，例如 `[fire, water]`。

这套数据模型很直接：规则层不需要节点，不需要贴图，只处理字典和坐标。

## 6. 一次玩家操作怎样执行

以“玩家点击一个白格并转化为火”为例：

1. 玩家点击棋盘格。
2. `game_app.gd` 的 `_on_cell_pressed(pos)` 保存 `selected = pos`。
3. `_refresh_game()` 重绘棋盘和侧栏。
4. `_render_side()` 调用 `ChaosRules.can_convert(state, player, selected)`。
5. 如果合法，侧栏生成“转化火 / 转化水 / 转化风 / 转化土”按钮。
6. 玩家点击“转化火”。
7. `_queue_convert(FIRE)` 创建意图：

```gdscript
{"type": ChaosRules.CONVERT, "to": selected, "element": ChaosRules.FIRE}
```

8. `_queue(intent)` 调用 `ChaosRules.queue_intent(state, intent)`。
9. 规则层补上 `intent.player = state.planner`，消耗该玩家 1 点 AP。
10. 如果双方当前同序号行动都提交了，规则层调用 `resolve_step(state)`。
11. `resolve_step()` 检查冲突，没有冲突就执行双方意图。
12. `_apply_intent()` 再次校验合法性，真正修改棋盘。
13. UI 层 `_refresh_game()` 重新读取 `state` 并重绘。

关键点：UI 生成按钮前会做合法性判断，但真正执行时规则层还会再判断一次。这样即使 UI 出错或外部直接提交非法意图，规则层也能拒绝。

## 7. 同步回合制的核心

这个游戏不是“玩家 1 操作立即生效，再轮到玩家 2”。它是同步规划、同步结算。

规则层通过 `staged` 字段实现：

```gdscript
state.staged = {
	1: 玩家1当前待结算意图,
	2: 玩家2当前待结算意图
}
```

`queue_intent()` 做三件事：

1. 检查当前玩家是否还有 AP。
2. 把意图放入 `state.staged[player]`。
3. 如果双方都准备好，就调用 `resolve_step()`。

`resolve_step()` 做结算：

- 如果双方意图影响同一格，调用 `intents_conflict()` 判定冲突。
- 冲突时随机选择一方成功。
- 不冲突时双方意图都执行。
- 清空 `staged`。
- 检查胜利。
- 如果双方 AP 都用完，进入下一回合。

这样就模拟了“双方第 1 条指令同时执行、双方第 2 条指令同时执行”的结构。

## 8. 操作系统怎样分类

规则层把所有玩家操作抽象为意图类型：

```text
pass
convert
activate_fire
activate_move
synth
ether_synth
chaos_synth
split
revert
```

### 8.1 转化

`can_convert(state, player, target)` 判断目标是否合法：

- 己方还没有任何棋子时，玩家 1 只能从底行开始，玩家 2 只能从顶行开始。
- 己方已有棋子时，只能转化与己方棋相邻的白棋或敌方非混沌棋。
- 不能转化己方棋。
- 不能转化混沌棋。

执行时 `_apply_intent()` 会把目标改成己方基础元素棋。

### 8.2 火激发

火的目标由 `get_fire_targets()` 找出，只能是相邻白格。执行时最多扩散 `activation_strength()` 个目标：

- 普通火：1 格。
- 高阶火：2 格。

火不直接攻击敌方棋。敌方棋必须通过转化处理。

### 8.3 水激发

水是移动，不是复制，也不是交换。

`get_water_targets()` 用广度搜索找出指定步数内可达白格。路径只能经过白棋。执行后：

- 目标格变成移动棋子。
- 原位置变回白棋。

### 8.4 风激发

风是跳跃移动。

`get_wind_targets()` 从当前位置按四方向检查：

- 相邻格是踏板。
- 踏板不能是混沌棋。
- 踏板后方必须在棋盘内且是白格。
- 高阶风可以连续跳 2 次。

执行后同样是移动：目标格得到棋子，原位置变白。

### 8.5 土防御

土没有主动按钮。它在敌方尝试转化或合成土系目标时触发。

`_earth_defense_pass()` 负责判断是否突破：

- 基础土或含土复合棋需要 2 次累计处理，或周围有至少 2 枚己方棋。
- 高阶土需要 4 次累计处理，或周围有至少 4 枚己方棋。
- 没突破时记录在 `state.sieges`。

### 8.6 合成

普通合成由 `SYNTH` 意图处理：

- 来源格变白。
- 目标格生成结果棋。
- 同元素基础棋生成高阶棋。
- 异元素生成复合棋。
- 复合棋用 `slots` 保存元素。

四元素合成以太由 `find_ether_synth()` 判断：锚点和相邻己方基础棋必须凑齐火、水、风、土。

四以太合成混沌由 `find_chaos_synth()` 判断：锚点和相邻以太棋凑够 4 枚。

### 8.7 拆分与还原

`can_split()` 先检查：

- 必须是己方棋。
- 不能是白棋。
- 不能是混沌棋。
- 周围白格数量要足够。

`split_need()` 决定需要几个白格：

- 高阶棋需要 1 格。
- 复合棋至少需要 2 格。
- 以太棋需要 4 格。
- 基础棋不能拆。

还原只允许己方普通基础元素棋，执行后变白。

## 9. UI 怎样根据规则生成按钮

行动按钮全部在 `_render_side()` 中生成。它不会手写固定按钮列表，而是根据当前选中格和规则层判断动态生成。

核心逻辑：

```text
先生成 PASS
如果没有选中格，结束
如果 can_convert，生成四个转化按钮
如果 can_activate，按元素生成激活按钮
如果 can_synth_source，生成合成按钮
如果 find_ether_synth 有结果，生成四元素合成以太按钮
如果 find_chaos_synth 有结果，生成合成混沌按钮
如果 can_split，生成拆分按钮
如果 can_revert，生成还原按钮
最后生成认输按钮
```

这就是 UI 和规则解耦的关键：UI 不自己复制复杂规则，只问规则层“当前状态下能不能做”。规则变了以后，只要规则 API 返回结果正确，按钮就会随之变化。

## 10. 目标选择模式

有些操作不是点按钮就能立即提交，例如水移动、风跳、合成、火扩散都需要再选目标格。

UI 用这几个变量管理目标选择：

```gdscript
var target_mode := ""
var target_context := {}
var valid_targets: Array[Vector2i] = []
var chosen_targets: Array[Vector2i] = []
```

流程：

1. 玩家点击“激活水”。
2. `_prepare_activation()` 通过规则层拿到合法目标。
3. `_begin_target_mode("move", context, targets)` 进入目标选择。
4. `_render_board()` 把 `valid_targets` 里的格子画成高亮。
5. 玩家点击目标格。
6. `_handle_target(pos)` 检查是否在合法目标中。
7. 生成具体意图并交给规则层。

这套机制同时服务合成、水、风、火多目标选择。

## 11. 棋子贴图怎样映射

棋子实际数据不保存贴图路径。UI 渲染时根据棋子数据选择贴图。

`_piece_texture(piece)` 规则：

- `kind == composite` 时调用 `_composite_texture(piece)`。
- 基础元素且 `tier > 1` 时使用 `piece_fire_high` 这类高阶贴图。
- 其他按 `piece_<kind>` 找贴图。

复合棋贴图根据元素组合选择：

```gdscript
var key := "piece_composite_%s_%s" % [ordered[0], ordered[1]]
```

`ordered` 按 `BASE_ELEMENTS = [fire, water, wind, earth]` 排序，所以火水和水火会映射到同一张 `piece_composite_fire_water.png`。不同组合会映射到不同颜色素材。

## 12. 教程系统实现

教程目前是应用层功能，不改变规则层。

`show_tutorial()` 显示分类说明和 8 个练习按钮：

- 转化
- 火激发
- 水激发
- 风激发
- 合成
- 拆分还原
- 以太混沌
- 同步冲突

点击练习后调用：

```gdscript
start_tutorial_lesson(lesson_id)
```

它会：

1. 设置 `tutorial_mode = true`。
2. 固定创建 5x5 棋局。
3. 设置双方 AP。
4. 调用 `_setup_tutorial_lesson(lesson_id)` 放置预设棋子。
5. 显示对局界面。
6. 自动选中关键格。
7. 在日志里写目标、规则、观察点。

教程关卡的本质不是独立关卡文件，而是“用规则层 API 构造一个特殊初始状态”。

## 13. 美术资产生成原理

美术不是手绘导入为主，而是 `generate_pixel_assets.py` 用 Pillow 画出来。

它的结构大致是：

```text
颜色表
  -> 基础绘图函数：new / save / rect
  -> 棋子函数：circle_piece / composite_pair_piece
  -> 棋盘函数：tile / board_frame
  -> UI 函数：ninepatch / icon 绘制
  -> generate() 批量输出 PNG
```

棋子都使用统一圆形像素棋子结构：

- 黑色外轮廓。
- 纯色主体。
- 高光和暗部弧线。
- 高阶棋额外外环。
- 复合棋使用组合专属主色，并在底部标出两个元素小色块。

这能保证所有棋子风格一致，同时让不同复合棋颜色不同。

运行时只加载 `assets/pixel`，所以如果重新生成资产，一般流程是：

1. 修改 `plan/scripts/generate_pixel_assets.py`。
2. 运行生成脚本输出到 `plan/assets`。
3. 同步需要的 PNG 到 `assets/pixel`。
4. Godot 重新导入 `.import`。
5. 跑截图测试确认 UI 不溢出。

## 14. 测试体系怎样保证项目可用

这个项目的测试分三类。

### 14.1 规则测试

运行：

```powershell
& 'D:\Program Files\Godot_v4.6.2-stable_mono_win64\Godot_v4.6.2-stable_mono_win64_console.exe' --headless --path 'D:\Godot_Project\chaos' --script 'res://scripts/tests/rules_smoke_test.gd'
```

目标是保证规则层不被 UI 影响。它直接创建 `state`，直接调用 `ChaosRules`。

### 14.2 应用流程测试

运行：

```powershell
& 'D:\Program Files\Godot_v4.6.2-stable_mono_win64\Godot_v4.6.2-stable_mono_win64_console.exe' --headless --path 'D:\Godot_Project\chaos' --script 'res://scripts/tests/app_flow_smoke_test.gd'
```

目标是保证主场景能加载、核心页面能切换、对局界面能生成、关键按钮存在。

### 14.3 截图与布局测试

运行：

```powershell
& 'D:\Program Files\Godot_v4.6.2-stable_mono_win64\Godot_v4.6.2-stable_mono_win64_console.exe' --path 'D:\Godot_Project\chaos' --script 'res://scripts/tests/app_screenshot_test.gd'
```

注意这里不要加 `--headless`，否则 dummy 渲染可能导致 viewport texture 为空。

它会保存截图并检查所有控件没有越界。UI 或资产变化后，这个测试非常关键。

## 15. 如果你要从零复写，推荐顺序

不要一开始就写完整 UI。推荐按下面顺序复写：

1. 建 Godot 项目，设置主场景为一个 `Control`。
2. 写 `ChaosRules.new_state()` 和棋子结构。
3. 写基础工具函数：`get_piece`、`set_piece`、`neighbors`、`piece_elements`。
4. 写转化规则和 `queue_intent()`。
5. 写 `resolve_step()`，先支持 PASS 和 CONVERT。
6. 做一个最简单的棋盘 UI：GridContainer + Button。
7. 点击格子后根据 `can_convert()` 生成转化按钮。
8. 加火、水、风、土规则。
9. 加合成、拆分、还原、胜利。
10. 加贴图映射和像素素材。
11. 加教程预设局面。
12. 最后补规则测试、流程测试、截图测试。

这个顺序的重点是：先让规则状态能跑，再让 UI 展示状态。不要反过来先堆界面，否则规则漏洞会很难修。

## 16. 改功能时应该改哪里

### 想改规则

优先看：

- `混沌棋规则.md`
- `scripts/rules/chaos_rules.gd`
- `scripts/tests/rules_smoke_test.gd`

流程应该是：先明确规则文档，再改规则层，最后补测试。

### 想改界面

优先看：

- `scripts/app/game_app.gd`
- `scripts/tests/app_flow_smoke_test.gd`
- `scripts/tests/app_screenshot_test.gd`

UI 改完必须跑截图测试，并查看 `outputs/test_screenshots`。

### 想改棋子或棋盘美术

优先看：

- `plan/scripts/generate_pixel_assets.py`
- `assets/pixel`
- `plan/assets`

颜色、形状、九宫格按钮都在 Python 生成脚本里集中控制。

### 想改教程

优先看：

- `show_tutorial()`
- `start_tutorial_lesson()`
- `_setup_tutorial_lesson()`

教程本质是预设棋盘状态，不应该复制规则逻辑。

## 17. 当前实现的关键原则

1. 规则层是权威，UI 只是调用者。
2. 状态用普通字典表达，方便测试和构造教程局面。
3. 玩家操作统一抽象成意图，由规则层排队和结算。
4. 同步回合制靠 `staged` 实现，而不是靠立即轮流改棋盘。
5. UI 动态生成行动按钮，按钮存在与否来自规则层判断。
6. 棋子显示和棋子数据解耦，数据只保存类型、归属、等级、元素槽。
7. 资产通过脚本生成，保证像素风一致和可重复调整。
8. 测试分规则、流程、截图三层，分别守住逻辑、入口和布局。

## 18. 一句话总结

这个项目的核心可以概括为：

```text
Godot 单场景应用层负责显示和输入；
ChaosRules 规则层负责状态和结算；
玩家点击被转换成 intent；
双方 intent 进入 staged；
resolve_step 同步结算并更新 state；
UI 每次只根据 state 重新渲染。
```

理解这条链路后，就可以按同样架构写出一个简化版混沌棋。
